# Island — Wave Seven status

The island reads this file from the repo. It is the only "done" the island accepts.

One row per phase. `status` is one of NOT-STARTED · IN-PROGRESS · DONE · BLOCKED · RETIRED.
`evidence` is a commit sha plus the test name that proves the done-means.
BLOCKED rows name exactly what is needed, and from whom.
Updated in the same commit as every phase close.

> ## ✅ SESSION ONE IS ON TRUNK AND LIVE IN PRODUCTION
>
> **Read it at `mvp-launch`**, tip **`2dc2fa39`** (PR #446, merged 2026-09-06). Nothing to
> clone specially any more; the island reads trunk. Production deployed from that sha and
> is serving it now.
>
> CI at merge: **18 success · 12 skipped · 0 failed**, including both E2E suites and the
> Anvil fork money-paths run. Trunk's PR #443 (multichain liquidity) was merged IN first,
> so nothing of it was reverted.
>
> **Elements DONE and live: J · G · M · B (all but the ladder and wallet fill) · C (the
> three paths).** Still open: A · D · E · F · H · I · K · L · N · O and the C/D gate merge.

Opened 2026-09-06 against base `8b6144ae` on `mvp-launch`, from
`jbi-greencifer-wave7-share-v1.3.pdf` (§0–§9, elements A–P), and amended from
`jbi-greencifer-wave7-answer1-share-v1.pdf` (answer one) the same day.

**Answer one resolved all four island asks**, and its rulings are folded in below: the
proxy's allowlist is adopted as the route contract (row 06); wave six's and wave four's
phase lists are opened as their own files; the painted card URL is not yet published;
`/api/flames?token=` does not exist and must not be mocked against a guess.

**Answer two (2026-09-06) read the pushed branch at `9a6ba2b7` and closed the rest.** All
seven flagged decisions are now ruled (see Decisions). Both corrections this venue sent back
were **owned by the island, not argued**: W4-02 had two sites because the island's grep for
"half a year" missed "half-year", and wave six row C was ruled from the very line that forces
the tab on. Three rows are rescoped by it: **N** gets its own `?resource=tape` door, **O**
stops being a receipt and becomes one inline line, and **P** is unblocked on the venue's own
Memetics Curve rail, which does expose a creator where the Doppler ledger does not.

This file opened the wave at `5f77da9e`, as §7 requires, before any code moved, and was
amended at every phase close rather than written once. Session one closed **J, G, M, C's three
paths and all of B but the ladder and the wallet fill**, and they are live (see the banner).
An evidence cell stays empty until a sha exists.

## What the opening pass established

Before any edit, all 16 elements were verified claim-by-claim against `8b6144ae`. **250
concrete file:line claims** were checked by reading the files: **155 CONFIRMED · 11
ALREADY_FIXED · 11 LINE_DRIFTED · 36 WRONG · 37 NOT_FOUND.**

The directive's recon is real and current — `VenueDoors.tsx:27` builds
`grayscale(1) brightness(${k})` exactly as written, `AppLoader.tsx:104` gates the click on
exactly `hold | textForm | vortex`, and `AppLayout.tsx:139` reads `tf_loaded` exactly as
described. Its paths omit the `frontend/` prefix and its line numbers have drifted
(`api/aggregator.js:313` is `frontend/api/aggregator.js:303`), so **every anchor is
re-verified at edit time**, per wave five's intent-map method. The PDF text layer is
corrupted the same way wave five's was (`ST A TUS`, `BA YLA`, `TW AB`, every curly
apostrophe flattened), so **no copy string is lifted verbatim from extraction** — each is
normalised against the existing `VENUE` constants, and anything ambiguous is asked, not
guessed.

## The island's upstream was probed live, and it is up

Six of the "does this exist" blockers were closed by reading the island directly rather than
asserting from the repo. Read 2026-09-06:

| probe | result |
|---|---|
| `GET /api/heat/0x279E7CFF…` (our own pinned reference address) | **200** — and the envelope **does** carry `x_handle` (`null` on this cold address, but the key is served) |
| `GET /api/flames?limit=3` | **200** — shape is exactly §4.1's: `x_username · x_pfp · degrees · tier · held_since_unix · token_count · wallet_count · person_id`, plus `as_of_unix` |
| `GET /flames` · `/register` · `/heat` | **200 · 200 · 200** — none is a dead link |

Two findings that change the work:

1. **The §4.1 strip is a real privacy requirement, not a formality.** The live board serves
   `person_id` as a genuine UUID and `wallet_count` up to 14 on the top flame. Those two
   keys link many wallets to one human. The proxy strips them **before** responding, and the
   unit test asserting neither key survives is the structural guarantee — exactly as the
   directive designed it. That test is non-negotiable.
2. **The two endpoints disagree on the field name**, and the directive is right about both:
   `/api/heat` serves `x_handle`, `/api/flames` serves `x_username`. Anything reading both
   must not assume one name. `x_pfp` is served and is **not** in §4.2's type — it is not
   painted this wave.

Element K is unblocked by the same probe: the island's published law page carries its own
sentences to lift — *"The island measures one thing: held time."*, *"TWAB is your
time-weighted average balance: your share of a token's supply, averaged over your whole held
time."*, *"the Apes carry triple weight, JBM and BAYLA carry their edge, the home team leans
warm."* That last grammar directly retires `HeatCard.tsx`'s "The period the average is taken
over has not been published."

## The mandated order is not safe as written

§0 orders A+E first, then B/G/M/N, then C/D/O/P, then F/H/I/K. Four dependencies the
directive missed, each verified by reading the files:

1. **A's gate depends on B.** A's done-means includes `/?heat=<address>` cold-mounts no
   canvas. No `?heat=` reader exists anywhere in `frontend/src`. That clause cannot be
   written until B ships the param.
2. **A alone breaks the art rule.** Cutting `T_ART_COUNT` 4→1 removes three gallery pieces
   from the arrival. The repo never removes art without an additive home, and A's own escape
   hatch is element G's "Watch the arrival" mount — which is phase 2. **A must carry G's
   film mount, or A does not ship.**
3. **C and D are one edit, not two.** The four blocks C deletes are **ungated today**
   (`HomePage.tsx` 780 / 952 / 1000 carry no `bungalowIdentity` gate; only FAQ at 1046
   does). So C is "add a gate", and D changes that same gate on the same lines in the
   opposite direction. Sequenced, whichever runs second reverts the first.
4. **`id="hall"` does not exist.** C, G and H all link to `/#hall`; `VenueDoors.tsx` carries
   no `id` at all. It is a prerequisite of the earliest of the three, not of C.

## Rows

| phase | status | evidence | notes |
|---|---|---|---|
| 00 wave six absorbed | RETIRED | this file | §0 states wave six never landed and its still-true parts ride inside this wave. **The wave six directive is not in this repo** — `rg 'wave six\|W6-'` over `docs/` and `frontend/src` finds only `BATTLE_PLAN.md`'s unrelated venue roadmap. Recorded as a pointer only; we cannot state what was absorbed beyond element I's own aside ("wave six B and D"). **Needed from the island:** wave six's phase list, or confirmation that A–P supersede it wholesale. |
| A the curtain, not the wall | **DONE** — measured, not summed | `ae132a2a` · `b09e6de6` · `a6a3d293` (choreography) · this commit (the deadline) · `skip.test.ts` (30) · `curtain.test.ts` (15) · `curtainDeadline.test.tsx` (8) · `771fe3fa` (the mark + the gate) · `e2e/arrival.spec.ts` (16) · mutation-verified ×23 |✅ **A1, the decision:** a room deep link and a `?heat=` read no longer wait behind the venue's film, matched on the PATHNAME (not `getBungalowIdentity`, which returns null on a cold `/bayla` at loader-mount time) against the real `BUNGALOWS` registry, so it cannot drift from the routes. Neither CONSUMES the arrival. The durable record moved to localStorage (once per browser, not per tab) while sessionStorage stays honoured, which kept the art studios' iframe suppression AND all three e2e seeders working untouched. ✅ **A2, the retirement:** the replay easter egg is gone as ruled — and the nav logo, which lived INSIDE it, was re-homed onto the way-back link first, with its test helper, a Playwright script and a stale comment moved with it. ✅ **A3, THE WALL IS DOWN:** the overlay is `pointerEvents: 'none'`, so the hero, its instrument field and the bar are interactive from first paint; Skip and Mute opt back in; and **any** pointerdown, touchstart, keydown, wheel or scroll lifts it at once, bound passively (never blocks scrolling) and on capture (a stray `stopPropagation` cannot strand it up). Mutations ×10 across the three: 14 / 3 / 3 / 1 / 3, then 1 / 1 / 1 / 1 / 1. **One mutation SURVIVED first time and the guard was rewritten because of it** — a whole-file grep for `pointerEvents: 'none'` matched the comment explaining it, so deleting the real line reddened nothing; the assertion is scoped to the overlay's own style block now. ✅ **A4, THE CHOREOGRAPHY — and no art was removed to get it.** One `AppLoader`, two arrivals, chosen by a `full` prop. The CURTAIN (default) is `CURTAIN_TIMING`: 400 ms void, ONE piece at 1200 ms, then straight to `textForm` and out through the existing `skip` dissolve — no shatter, no vortex, no hold, no crack. `createParticles` still seeds from the art, so the picture dissolves INTO the wordmark rather than being replaced by it: the same effect the film reaches the long way round. The FILM is `FILM_TIMING`, byte-for-byte the old constants, and a test asserts it still equals them so nobody can quietly edit the four-piece arrival. ✅ **A5, THE FILM'S HOME (element G's mount, which A ships WITH):** "Watch the arrival" in the Island lobby, beside a "The bungalows" link to `/#hall`, **lazily imported** so the ~93 KB of choreography is fetched only by a visitor who asks — PERF-16's whole point, which an eager import here would have undone in a different chunk. The film opts back into pointer events (its click-to-crack is part of the art) and **only Escape dismisses it**, because somebody chose to watch it and a stray scroll should not end it. **Two of the seventeen mutations SURVIVED first time and both guards were rewritten:** a whole-file grep for `pointerEvents: 'none'` matched the comment explaining it, and a whole-file grep for the phase names passed however the branch was wired. Both are scoped to the decision now, not the vocabulary. 🔴 **A4 WAS WRONG AND THE ISLAND MEASURED IT.** The block claimed "curtain gone by ~2400 ms, budget pinned at 3000". The island ran branch `d51daa66` cold in Chromium, sampling every 100 ms: the curtain lived **~4,250 ms warm** and **~6,100 ms behind a 2 s-delayed image**. The sum left out the two longest legs — `phases/textForm.ts:9` was `const textDuration = 2000` (a literal the film and the curtain SHARE, and the curtain routes straight into it) and the preload gate holds the tick for up to `PRELOAD_BUDGET_MS = 2500`. Real range 4,000–6,500 ms. **A guard that "pins a promise arithmetically" is only as honest as the terms somebody remembered**, which makes it the same class as the two vacuous guards mutation caught on this very element. The island owns its half: the master said "about 2,500 ms" and "a 600 ms dissolve", both written without reading `textForm.ts:9` or the dissolve that spends 400. Its new law, and ours: **a duration is read from the line that spends it, never added from a constants file.**<br><br>✅ **FIXED, as a deadline rather than a sum.** `textForm` is now a leg of `ArrivalTiming` (film 2000 byte-for-byte, curtain 800) and `SKIP_DISSOLVE_MS = 400` is spent from constants, so the four-leg sum is 2,800. But the PROMISE is a timer armed at mount, curtain only, in its own effect — never inside the canvas effect, which returns early with no 2D context. **TWO stages, and the second is load-bearing:** `skipIntro` only sets the phase, and the dissolve+finalize live in the tick, so on a machine with no context a lone `skipIntro` would leave the overlay up forever — on exactly the machines a deadline is for. A second timer ends it unconditionally at the budget. Proven in jsdom with `getContext` mocked to **null** and a preload that **never resolves**: `onComplete` fires once by 3,000 ms. Removing the unconditional end reds 4 tests, which is why it is there. ✅ **Mute is gone from the curtain** — the island measured it LIFTING the curtain, because the window listeners are `pointerdown` on capture and run before the button's click, so its `stopPropagation` cannot stop a finished event; and on a curtain any gesture ends, audio can never start, so there was nothing to mute. Film keeps it. ⏳ **Owed:** the e2e (answer four §2 supplies the two opt-outs exactly), and the measured lifetime replaces every summed number in the block from here. **Particles: 2,000 desktop / 1,000 mobile** (`AppLoader.tsx:320`, `:398`); the throttled-CPU profile the master asked for was **NOT RUN**. Anchors confirmed: `AppLayout.tsx:185` `<AppLoader onComplete>`, `AppLoader.tsx:104` click phases, `constants.ts:74-86` timings (current 1500 / 4 / 2600, `T_TEXT_END` 14500). Two corrections: children **already** render outside the lazy boundary (`loader/index.tsx:51`) so the gap is visibility, not mounting; and the art pieces **do** carry titles (`phases/art.ts:76-87`), contra "four art pieces with no words". 🔁 **Re-mapped at trunk `2dc2fa39`, and this file's own count was wrong.** The `tf_loaded` move is **NINE statements across SIX files**, not seven and not two: `AppLoader.tsx:568,573,609` · `skip.ts:25,29` · `AppLayout.tsx:139` · `TopNav.tsx:210` · **`ArtStudioPage.tsx:58`** · **`BungalowArtStudioPage.tsx:177`**. The two art-studio pages are what every earlier count missed, and they are not decoration: each writes the key to suppress the splash **inside a same-origin preview iframe**, which shares sessionStorage with the top window. Move the key to localStorage and both studios keep working only if they are moved with it. Reds `loader/index.test.tsx:52,96` and vacuums three e2e seeders.<br><br>🔴 **RETIRING THE EASTER EGG IS NOT SELF-CONTAINED, AND THE NAIVE CUT DELETES THE NAV LOGO.** The island ruled it retired (one door for the film), but the removal target `TopNav.tsx:203-242` **contains the nav logo `<img>` at `:229-234`** — the logo lives inside the replay button. Deleting the button as written removes the venue's mark from the bar, which is an art removal and forbidden without an additive home. The logo must be re-homed FIRST (the plain `<Link to="/">` at `:258` is the natural host), then the replay behaviour removed. Three external references also break: `TopNav.navLogo.test.tsx:56-61`, whose `navLogo()` helper finds the logo THROUGH `getByLabelText('Replay splash screen (full reload)')` and carries all four of its tests; `scripts/verify-bungalows.mjs:141`, which `$eval`s `button[title^="Replay splash"] img`; and a stale comment at `bungalows.ts:372`. All four must move in the same commit. **RULED 2026-09-06 (owner): A ships only together with element G's "Watch the arrival" full-film mount**, so no art is ever homeless — part of G is pulled forward into A's change. The replay easter egg's fate is still open.<br><br>🔴 **THE COMMIT THAT ARMED THE DEADLINE BROKE ONCE-PER-BROWSER, AND THE ISLAND MEASURED THAT TOO.** `markArrivalSeen()` lived only inside the animation tick, at `AppLoader.tsx:689,:694,:730` — the exit paths and the last frame of the dissolve. The deadline made all three unreachable: `skipIntro` at 2,600, a dissolve that needs its full 400, and the `finalize` timer at 3,000 winning the race against the very frame that would have marked the arrival. So `tf_loaded` was never written and the curtain played on EVERY load, warm or slow, on every machine. Five runs of five, `localStorage` and `sessionStorage` both null in all five. **Our guard for this read the skip decision instead of loading a page twice**, which is the same class as the summed budget it had just replaced. The island owns its half and says so: answer four §1.3 named `finalize` as the deadline's end without saying where the mark lives.<br><br>✅ **FIXED, seen means shown.** The mark happens at mount, curtain only, in the effect that arms the deadline. A browser that has drawn the first frame has met the venue; how the curtain ended does not change that. The film's three in-tick calls stay — it has no deadline, so its own exits are reachable, and a mount-time mark there would consume the arrival of somebody who closes the tab two seconds in. Ordering is safe and was checked rather than assumed: `AppLayout`'s `freshSplash` reads through a `useState` initializer, React finishes the whole render pass before any effect runs, and the overlay is lazy besides.<br><br>✅ **THE PRELOAD GATE IS GONE FOR THE CURTAIN** (answer four §1.1, which had not been applied). A 2,500 ms wait under a 3,000 ms deadline is a black screen and nothing else — the island screenshotted 100% black pixels at +1,100 and +2,900 ms, the name never forming. The curtain now starts its void at mount with `images: []` and the picture lands INTO it while the void plays; the branch at the void's end (`AppLoader.tsx:440`) was already written for both outcomes. A picture that turns up after the void has ended is not adopted — restarting choreography the deadline has half spent is a worse arrival than the particle wordmark. `PRELOAD_BUDGET_MS` stays for the film, whose wait is what the viewer asked for.<br><br>📏 **THE NUMBERS, MEASURED FROM THE PAGE, NEVER ADDED.** Chromium, `e2e/arrival.spec.ts`, clock stamped by a `MutationObserver` inside the page from the overlay's mount to its removal (not `Date.now()` around a `goto`, which would charge navigation to the curtain): **curtain lifetime 2,886 ms warm**; **1,690 ms with the art stalled 2,500 ms**; **canvas at +800 ms under that stall: 309 of 14,400 sampled pixels lit** — the void's gold line, drawn. **Both fixes were seen red first**: the two once-per-browser guards fail with the mark moved back into the tick, and the slow-art canvas reads **exactly 0 lit pixels** with the gate restored, reproducing the island's screenshot to the pixel.<br><br>✅ **THE §2 E2E LANDED AND IT TAKES ALL THREE REFINEMENTS.** Fixture-free and `reducedMotion: 'no-preference'`, because the config sets `'reduce'` globally and the wallet fixture seeds `tf_loaded` — a curtain assertion in any other spec passes vacuously. 16 tests. Once-per-browser is asserted **after the deadline path with no gesture anywhere**, then again in a SECOND TAB, which is the half that says browser rather than tab. **Two defects in the spec's own first run were found and fixed rather than papered over:** the clock observed `document.documentElement`, which can be null when `addInitScript` runs, so `observe()` threw and the whole init script died SILENTLY; and the overlay sweep flagged the sr-only "Skip to main content" link, whose centre is off-screen where `elementFromPoint` honestly answers null. ⏳ **Owed by the island:** its own re-run of the slow-image screenshots at this tip.<br><br>🔊 **AND THE E2E FOUND ONE MORE, BY REFUSING TO CALL IT FLAKE.** The keypress guard measured **711 ms** and once **967 ms** from the press against the island's ruled 600, while passing 5/5 in isolation — the shape of a test somebody relaxes. It was not flake. `skipIntro` called `initAudio()` unconditionally, so on the curtain the ONLY gesture that reaches it — the one dismissing it — constructed an `AudioContext` and fetched `/audio/ambient-loop.mp3` for an overlay 400 ms from gone, disposed unheard. Constructing an AudioContext blocks the main thread while Chromium starts its audio thread, at exactly the moment the dissolve needs frames. **This is the island's own Mute ruling one line further in** — "on a curtain any gesture ends, audio can never start, so there was nothing to mute" — and it condemns this call in the same sentence. Gated to the film, where a gesture is a viewer engaging with something that goes on playing. **Now 448 ms**, then 451 / 446 / 482 / 457 across four more runs: the 400 ms dissolve plus a frame, against the island's measured 496. Pinned in `curtain.test.ts` scoped to `skipIntro`'s own body (a whole-file assertion would pass on `handleClick`'s call, which is a different decision on a path the curtain cannot reach) and mutation-verified. The keypress test also gained the invariant no machine can move: it ended **on the key, not on the deadline** — without that, a box slow enough to converge the two endings would go green exactly when the test stopped meaning anything.|
| B the instrument is the hero | IN-PROGRESS — **plumbing DONE** | `heatOracle.test.ts` (75, mutation-verified ×2) | The wave's engine. **B1 landed:** `x_handle` now rides the reading. `HeatReading.xHandle` is stored BARE (no `@`), so §5's "never compare handles with the @ in place" is structural rather than a discipline — there is one form in memory and painting adds the single `@`. `normalizeXHandle()` is exported from `heatOracle.ts` as the ONE implementation, so B, G's board and N's tape cannot drift. **It validates as well as normalises, and that half is a security boundary:** the handle becomes an `href` to x.com and a public byline, and `//evil.example`, `https://…`, `../../login`, `javascript:`, query/fragment smuggling and an RTL override all survive a naive `replace(/^@+/, '')`. Only X's own charset (1-15 of `[A-Za-z0-9_]`) is accepted; anything else reads as unnamed, which is honest, where a spoofed byline is not. A missing handle is explicitly NOT an envelope failure. Mutations: dropping the validation reds 13; stripping one `@` instead of every `@` reds 1. Typecheck 0 (one construction site fixed, `launchPricing.honesty.test.ts:146`), eslint clean, 1149/1149 across heat + launcher.<br><br>**B2 landed — the read surface.** `HeatCard` now paints the island's order (tier · days · degrees · since · tokens), the delta, the cold read and the name-or-door. **`HeatCard` had NO component test before this**, so its whole result area could have been recut on a green suite that never looked at it; `HeatCard.test.tsx` (18) is new and pins the order, both honesty states and the byline. Days are counted `held_since` → `as_of`, **never to our clock** — the span since the island's last reckoning is time it has not counted, and adding it would state a number the oracle never served. The delta is derived during render and remembered in an effect, so first read prints nothing and the second compares; storage is bounded at 24 addresses and every read/write is try/caught (private mode prints nothing, never an error). The cold read gives the sentence plus one door and suppresses ladder, delta and byline. Mutations ×3: counting days to our clock reds 1, dropping the hall link reds 1, remembering before comparing reds 5. Typecheck 0, eslint 0 errors, **1016/1016 across 102 component + heat files**. Also added `id="hall"` to `VenueDoors` (the missing anchor C, G and H all need) with `scroll-mt-24` so the heading clears the sticky bar.<br><br>**B3 landed — the hero is the instrument.** `VenueHero` is three lines now (h1 · `heroPlain` · `heroHook`) and the address field renders always, directly under the hook: a disclosure that hid the answer to the sentence immediately above it was a wall with a handle on it. The three CTAs are gone and **none is orphaned** — verified before removing, not assumed: "Launch on Heat" and "Scan any token" are both in the bar (`navConfig.ts:353`, `:569`), the picker keeps three other doors (`TopNav:292`, `Footer:175`, `BungalowDoorLanding:193`), and the hall sits directly below. `VENUE.heroCopy` was NOT deleted; it now opens `/start`, which this same hero already links to and which rendered no such lede before. The two per-wallet honesty lines are under the card, and `heatPlain`/`heatExample` moved below it rather than being dropped. **B4 landed — the insertion rank.** An UNNAMED flame now reads "Against the island's board right now, this wallet's number would sit at #n of m", computed in the browser from `limit=500` **without** `claimed` (a rank against named flames only would flatter the number), never persisted, never called heat. A NAMED flame gets no such line and the board is not even read for one: their real position is the island's to state, not ours to simulate. A tie sits BELOW the flame already on the board, because ranking above it would assert a placement the island never made. Board off or unreachable, the line is simply absent. **B5 landed — `?heat=` and the share.** `initialAddress` seeds the field and reads on mount while leaving it EDITABLE (unlike `address`, which pins and hides the form): somebody who followed a stranger's number should be one paste from their own. An address that is not one reads as the field's own invalid state rather than being silently swallowed. The share button now ships, because element M shipped first and `/read/<address>` resolves — posting it earlier would have had holders broadcasting dead links. Text is §4.4's exactly, off `SITE_URL` rather than a literal host, and there is **no share on a cold read**: a cold wallet has nothing to post and asking would be the one moment this instrument shames someone. Mutations: showing the share on a cold read reds 1, breaking the seeded auto-read reds 3. **Still open in B:** the five-rung ladder and the wallet fill. **The share button is deliberately NOT built yet** — it posts `memetics.finance/read/<address>` publicly, and that route does not exist until element M, so shipping it first would have holders broadcasting dead links. Same class of error as O. It lands with M or after it. |
| C the home, cut to the line | **DONE** — five gates, closed | `ThreePaths.test.tsx` (7) · `e2e/arrival-voice.spec.ts` (4) · `e2e/bungalow-doors.spec.ts` (24) · mutation-verified ×4 | `ThreePaths` is built and mounted on the venue arrival directly after the hall, on the same gate. It is where the hero's three CTAs went, and each card states its requirement at the point of intent. The launch floor is READ from `heatLaunchFloor()` at render, never typed — the one thing here that could rot silently, since a hardcoded 80 renders identically until the day an operator moves the dial and the home promises a different number than the gate enforces. Mutations: typing the floor reds 1, dropping a path reds 3, putting an em dash in venue voice reds 2. **Still open in C: the removals** (Launch & Verify, Ecosystem, Collection, FAQ, the hero trust-badge row), which are **one gate edit shared with D** — see below.<br><br>**Merge with D — one gate edit on one line range.** Rated M by the directive; it is the riskiest element in the wave. `trustCopyHonesty.test.ts:125` requires `<Link to="/jbm"` to survive in `HomePage.tsx` source, and that link sits at :975 **inside the Ecosystem block C removes**. Also orphans art slots `pageId="home" idx={9..14}` against the additive-art rule, and no HomePage render harness exists. <br><br>✅ **THE THREE GATES ARE IN.** `Launch &amp; Verify`, `Ecosystem` and `The Collection` were rendering on every surface — the island measured all three below the market card on `/bayla` and `/pepe`, so a resident's visitor read "Launch on Ethereum" and "Check a deployer" underneath someone else's token, then the venue's own gallery. All three now carry `IS_TOWELI_ARRIVAL && !bungalowIdentity`, the gate this file already uses for the farm's body. The venue arrival is hero → hall → three paths → board → footer.<br><br>**Launch &amp; Verify was written for a front door that funnelled everyone into farm/swap/dashboard, and it was the right answer then.** The three paths are that answer now: they carry launch and scan as deliberate choices with the requirement stated at the point of intent, directly after the hall. Two blocks offering the same two actions is not twice the invitation.<br><br>📏 **MEASURED FROM BOTH SIDES, BECAUSE A GATE AND A DELETION LOOK IDENTICAL FROM THE FRONT DOOR.** `arrival-voice.spec.ts` reads the venue arrival's whole rendered text after three scroll passes and finds none of the three, having first asserted the line IS there (hall and footer) so it is not a test that a page is empty; a second test then reads `/toweli` and finds **all three**, which is what makes this a gate. The thirteen-door sweep refuses them from the room's side too. Removing the gates reds the venue test and leaves the `/toweli` test green, exactly as it should. ⏳ **Open, and the island's to rule:** the FAQ teaser still renders on the venue arrival with its own venue-voice copy (`'Questions about the venue'`, `HomePage.tsx:1074`, gated `!bungalowIdentity` only). Answer five §3 names three blocks as C's gates and the block's own line says "hero, hall, three paths, board, footer" — those disagree about the FAQ. It is left rendering rather than cut on a guess, because cutting it makes its venue-voice copy unreachable and that is a deletion wearing a gate.<br><br>✅ **THE FOURTH GATE: THE FAQ TEASER, AND THE ISLAND OWNED THE AMBIGUITY.** I left it rendering and named the disagreement rather than guessing; the island's answer six settles it — answer five §3's "three blocks" sentence described the ROOMS (the teaser never rendered there, its gate was `!bungalowIdentity`), while the master's five arrival gates and C's own done-means always included it. Gated exactly like the other three. It is a gate and not a deletion: the answers live at `/faq`, the arrival's footer still links there (asserted), and inside /toweli it renders as before under `FAQ_INTRO`'s own headline. The venue-voice copy went with the gate rather than sitting behind an unreachable ternary.<br><br>🔴 **AND THE ISLAND'S BREAK-THE-FIX DID NOT BITE — WHICH WAS THE REAL FIND.** "Widen the gate and watch the venue assertion go red": widened, it stayed GREEN. The venue copy had left with the gate, so `'Questions about the venue'` is a string that now exists nowhere in the repo and an assertion about it **cannot fail**. A vacuous guard, written to prove a fix, in the wave whose whole discipline is catching vacuous guards. The teaser is identified by what it actually renders now (`FAQ_INTRO.headline`, imported so it cannot drift from its subject); the retired line keeps a second assertion beside it, which honestly cannot fail today and says so. Widened now, the venue test reds and the /toweli test stays green.<br><br>✅ **THE FIFTH: THE SECURITY BADGE MOVED TO THE CHECK OVERVIEW.** It sat between the hero ternary's close (`HomePage.tsx:390`) and the motion wrapper, so it rendered under all three voices — venue, /toweli and inside a room. It renders on `/trust` now, directly above the paragraph that already sends a visitor to Security and Risks: the page where somebody IS asking, which now leads with the checkable record instead of only linking to it. Every term stays checkable in one click and it still publishes no aggregate "findings resolved" count. The chain pills stay, as ruled. **The e2e moved in the same commit** — `gauge-voting.spec.ts` walked `/` for this badge; it walks `/trust` now AND asserts the arrival does not carry it, because without that second half re-adding the hero badge leaves everything green. Re-added, it reds. Two comments that described the old arrangement were corrected rather than left to rot (`HomePage.tsx`'s hero note and `BungalowHero`'s docblock, which said the same thing from the other side).<br><br>♿ **AND THE FIFTH GATE FIXED A REAL A11Y VIOLATION THAT HAD BEEN FILED AGAINST THE WRONG COMPONENT FOR MONTHS.** CI went red on the shared-chrome audit — not because something broke, but because `heading-order` had STOPPED being violated, and the repo's known-violation lists are exact both ways. `CHROME_KNOWN_VIOLATIONS` had carried it for as long as the list has existed, blamed on `components/layout/` because that is where the flagged node lives: the footer's first `<h4>`. **The footer was innocent.** `heading-order` judges a heading against the PREVIOUS one in document order, and the previous one was whatever the arrival's last section left behind — inside `main`, which that audit excludes from REPORTING but cannot exclude from the document. The arrival ended on the FAQ teaser's `<h2>`, so the real sequence was h1 → h2 → h3×3 → **h2** → h4: a jump straight from h2 to h4, skipping h3, and the footer wore it on every route's behalf. The arrival now reads h1 → h2 (the hall) → h3×3 (the three paths) → h4 (the footer). Read off the rendered page before and after, not deduced. The list is now empty rather than deleted, so the next regression lands there with the reason attached.|
| D the room, cut to the token | IN-PROGRESS | `d0ac425a` · `e2e/bungalow-doors.spec.ts` (20, thirteen doors) |✅ **The grid's gate is inverted and the rooms are clean.** The island is right that this row read NOT-STARTED while the code was one commit ahead — the same defect as code claimed without a row, and the same sentence this file already owns against element E. Fixed here.<br><br>📏 **MEASURED ON THIRTEEN DOORS, NOT A SAMPLE.** The sweep was one room and two strings while the block claimed thirteen; it is now generated from the `BUNGALOWS` registry (so a fourteenth door cannot slip past it) and reads the WHOLE rendered text after three scroll passes to the bottom, because half these sections are `whileInView` and do not exist in the DOM until they are scrolled to. Zero "TOWELI" and zero "Protocol Overview" on all twelve rooms — `/bayla /pepe /qr /mfer /bnkr /drb /bobo /jbm /soy /brainlet /rizz /nb1` — and `/toweli` keeps both, which is the half that makes the other twelve mean something: a grid simply deleted would be twelve greens and a poorer venue. `tegridy-bungalow` is seeded per door so the door's own persist-and-reload does not double the load; `nb1` is deliberately NOT seeded, being the quiet slot with `live: false`, which `byId` refuses.<br><br>⏳ **What remains, named:** the room's own read ("Your held time in &lt;SYMBOL&gt;", the breakdown row for the room's contract first and the whole flame second, plus the no-row and retired grammars); the pool panel or the one honest line; and the three blocks the rooms still render (Launch &amp; Verify, Ecosystem, The Collection) — those are C's gates and land with C. `lpEmissions.ts:81-85` is TOWELI-room copy and must be unreachable from the other twelve doors after this.<br><br>✅ **The three blocks fell with C, as the island said they would.** `Launch &amp; Verify`, `Ecosystem` and `The Collection` are gated to `/toweli` and the thirteen-door sweep now refuses them from every room alongside TOWELI and the Protocol Overview grid. What remains in D is the room's own READ — "Your held time in &lt;SYMBOL&gt;", the breakdown row for the room's contract first and the whole flame second, the no-row and retired grammars — and the pool panel or the one honest line.<br><br>🔴 **THE SCOPED READ'S RETIRED GRAMMAR COULD NOT HAVE BEEN BUILT, AND THE REASON WAS A DROPPED FIELD.** §D says "a row with `retired: true` reads the venue's existing retired grammar". `HeatBreakdownRow` had no such field: `heatOracle.ts` read six keys off each row and let the seventh fall on the floor. **The upstream has been sending it the whole time** — confirmed by reading the live envelope, not by reading the directive.<br><br>📏 **AND "RETIRED" DOES NOT MEAN "EXCLUDED".** Measured on a real 18-row flame (`?resource=heat` on `0xd71caf9f…`): the envelope's `degrees` is **1792.96**, all eighteen rows sum to **1792.96**, and the **four retired rows carry 155.61 of it** (BOBO 92.74, BOBO 56.54, ALPHA 4.34, SOY 1.99). A retired row is still the holder's held time and still inside their number. The obvious reading of the word is the wrong one, and copy built on it would tell somebody their time had been taken away — so the fact is written on the field itself, where the next person to render it will be standing. Parsed as `b.retired === true` rather than `!!b.retired`, because every string is truthy and a loose read would retire every row the day the upstream encodes it as `"false"`. Five guards, both mutations seen red.<br><br>⏳ **What is left in D, now unblocked:** the scoped read itself — the row whose `token_address` matches the room's contract painting its degrees and first-seen first, the whole-flame line second ("your whole flame reads &lt;degrees&gt;° &lt;Tier&gt;"), the no-row grammar ("This wallet holds no measured &lt;SYMBOL&gt; yet.") and the retired one — plus the pool panel or the one honest line. ❓ **For the island:** §D's retired grammar says "the venue's existing retired grammar", and the venue has no existing grammar for a retired ROW — the only retired thing here is a retired CLAIM (`HeatCard.tsx:729`), which is a sentence the island withdrew, not a token. Given the measurement above, the row's grammar has to say the degrees still count. Send the sentence and I will render it verbatim rather than invent one that speaks for the island about a holder's own time.<br><br>✅ **THE SCOPED READ IS BUILT.** A room answers its own question now: your held time HERE first, your whole flame second, in §D's order. Built as a MODE of the instrument rather than a second one — the directive's words are "the instrument, element B, scoped" — so the address derivation, the fetch, the freshness and the failure sentences are all the venue's, unchanged. An unreadable instrument in a room fails the way it fails everywhere and still never reads as a zero.<br><br>🔴 **MATCHED CASE-INSENSITIVELY, AND THAT IS LOAD-BEARING.** The registry holds EVM addresses lowercase and Solana mints in base58 WITH CAPITALS, and the island echoes back whatever it holds. A raw `===` finds no row for every Solana room and prints "This wallet holds no measured BAYLA yet." to somebody holding plenty. Guarded with a real mint; the case-sensitive mutation reds it.<br><br>**A retired row renders like any other**, carrying only the island's own word as a chip — because it still counts. ❓ The SENTENCE is still owed: §D asks for "the venue's existing retired grammar" and the venue has none for a ROW, only for a retired CLAIM. Not invented here.<br><br>👁️ **TWO DEFECTS THE UNIT TESTS COULD NOT SEE, both found by screenshotting the actual room:** the heading lived inside the RESULT block, so a cold visitor — which is every first visitor — saw an address field and a Read button with nothing saying what they read; and `embedded` drops the card's chrome, which is right in the gate and left this as bare white type over PEPE's full-bleed painting, unreadable. Both fixed, both guarded. ⏳ **Still open in D:** the pool panel or the one honest line.|
| E zero unasked overlays | **DONE** | `2056b313` · `fc0e51c7` · `862088d9` · `771fe3fa` (the sweep) |✅ Two of three sites landed: the MuseBubble mount is gone from every room and the component **deleted rather than exempted** (the reachability guard's own words), and the room's welcome is INVITED behind "About this bungalow" via `OPEN_BUNGALOW_ABOUT_EVENT`, one mechanism shared with the venue's welcome. ✅ **The third site is closed.** `InstallPrompt` was a `position: fixed`, `z-[9500]`, `role="dialog"` panel at `bottom-20` that opened itself the moment Chromium fired `beforeinstallprompt` — over the very buttons a visitor was reaching for. It is a **row in the footer** now ("Add to home screen"), mounted beside the Bungalows row; `PwaRuntime` keeps only the service-worker registration. **The offer decision is untouched**: `lib/pwa/install.ts` still rules, so the row appears only when the browser handed us a real unfired prompt. The dismiss control went with the banner (a quiet row does not nag) but `readInstallDismissed` is still honoured, so anyone who dismissed the old banner is not re-offered. The explanatory sentence went too — a row has no space for a paragraph — and the **honesty half of its test stays**: nothing may ever suggest the installed app works offline. New guard asserts it is a ROW on the rendered DOM (no `role="dialog"`, no element with `position: fixed`) rather than on a class string a rename could defeat; restoring the banner reds it. ⏳ **DONE still needs the §2 e2e** (zero open dialogs and no fixed element intersecting a hero button, on six cold routes). **Fair hit from the island: this row read NOT-STARTED while the code was two commits ahead** — a row that lags the code is the same defect as code claimed without a row. Anchors confirmed. Deleting the `MuseBubble` mount **reds two currently-green e2e outright**, not by flake: `bungalow-doors.spec.ts:116` expects the `— the island` byline on /pepe (it comes only from MuseBubble), and `ambient-bubble-taps.spec.ts:96` asserts the bubble rendered. Both must land in the same commit as the deletion.<br><br>✅ **THE §2 SWEEP LANDED, AND IT ASKS THE ISLAND'S QUESTION.** Six cold routes (`/ /bayla /pepe /launch /liquidity /island`), no seeds, no fixture, measured at 3,000 ms: zero `[role="dialog"]`, and `elementFromPoint` at the centre of every visible hero control returns that control.<br><br>🔴 **The first version was a box walk and the island was right to reject it.** It flagged any fixed box intersecting a hero button, which reds forever on the `position: fixed; inset: 0; z-index: 0; pointer-events: auto` background layers present on every route while they cover precisely nothing — and it had already flagged the consent bar for overlapping a footer link, which is inherent to any bottom bar. The honest question does not care what is fixed or what is painted where: if a visitor puts a finger on this control, do they get this control? A full-viewport backdrop that yields the hit-test passes, correctly.<br><br>✅ **AND THE SWEEP IS PROVED NON-VACUOUS.** The island's two mutations are "restore the install banner" and "re-mount MuseBubble", both deletions from this branch — reviving either to prove a test is a strange commit to live with. A seventeenth test plants the same SHAPE instead (a fixed panel, over a hero control, taking pointers) through the same code path that measures, so the two can never disagree about what a hero control is, and demands the sweep names it. The first attempt at this planted over the off-screen skip link and then honestly reported nothing covered; that is why planting and measuring are now one function.|
| F the launch page folds | **DONE** | `e2e/launch-fold.spec.ts` (4) · 90 e2e · 1,433 vitest | Anchors confirmed with drift. `heat-gate.spec.ts:136` pins the literal gate sentence — if F edits `gateDecision().detail` in `heatOracle.ts` rather than the render layer, F breaks its own phase. ✅ **ALL THREE DONE-MEANS, EACH MEASURED ON THE RENDERED PAGE.**<br><br>**Under 120 words: 148 → 108** at 1280×800. The header was 38 words carrying four claims stacked above a wizard nobody had reached; it is one sentence now, and every claim is still made in full behind the door.<br><br>**Every essay under one door, unchanged.** Byte-identical inside — which is the directive's own word and also what keeps four source-reading tests green. A `<details>`, not a conditional render, and that is load-bearing twice: keyboard- and SR-native with no JS, and a closed `<details>` keeps its children IN the DOM so the em-dash walk still counts them. `PostGraduationReattest` stays OUTSIDE on purpose — it is a working tool, and folding it would hide the unlabelled input that IS `/launch`'s declared `form-field-label` violation, reddening the a11y sweep for a reason unrelated to this element.<br><br>**The gate reads a pasted address.** The door had nothing to say to anyone without a wallet — the state every first visitor arrives in — while naming held time as the thing that matters. The instrument goes there whole. **It READS; it does not OPEN:** `prove()` needs a signature and a pasted address cannot sign, so the reading is advisory and the copy says so before somebody reads WARM and finds the lane shut.<br><br>⚠️ **THE TWO DONE-MEANS PULL AGAINST EACH OTHER**, now written where the next person will stand: the paste field is an input, a button and a caption ABOVE the wizard, and it took 108 back to **137** on its own. The gate's copy paid for it (40 words → 24).<br><br>🔴 **FIVE Tegridy strings, not three — and the fifth was found only after the guard was fixed.** My "zero Tegridy" assertion read `innerText`, which is LAYOUT-AWARE and omits whatever a closed `<details>` hides: it stopped covering the essays the moment F folded them and passed because it could not see them. On `textContent` it immediately found "Tegridy fee economics" inside the graduation panel. `TegridyV4Hook` and `TEGRIDY_V4_MIGRATOR_ADDRESS` are deliberately NOT swept and are excluded by name: those are what a contract and a constant are really called, and renaming them in copy would misname things an operator has to go and find.<br><br>The em-dash budget moves **32 → 31** in the same commit, because the header sentence carried one. The fold moved none.|
| G the board on the venue, and the flames proxy | **DONE** | `api/_lib/__tests__/flames.test.js` (36, mutation-verified) | **The proxy landed first, ahead of B**, because B's insertion rank reads it at `limit=500`: it is a prerequisite of the engine, not a peer. `api/_lib/flames.js` + the `?resource=flames` branch at `aggregator.js:326`, verified above `const provider` (:424). Gates copied from `heat.js`, not re-derived. ✅ **THE ALLOWLIST IS NOW THE ROUTE CONTRACT** — answer one adopted this shape for `/api/flames`: exactly `x_username · degrees · tier · held_since_unix · token_count`, with `x_pfp` dropped. Adding a key is a change to the island's contract, not a local edit. **The strip is an allowlist, not the directive's denylist** — a denylist holds only for the two keys known today, so a third identifying field the island adds later would reach a public board silently; the allowlist drops it instead. `x_pfp` is served upstream and deliberately not forwarded (off-origin avatar = a new CSP img-src entry and a viewer-IP leak; its own decision). Own rate-limit bucket (`identifier: "flames"`), so a board read never starves the tape's heat budget — element N's collision does not spread here. Mutation run: removing the strip reds 4 tests including the headline key assertion; restored, 36/36. Neighbours green (heat + all aggregator suites, 774/774). **The card landed too.** `flamesClient.ts` + `FlamesBoard.tsx`, mounted on the home at `limit=5` and opening the Island lobby at `limit=25`. The client keeps **three outcomes distinct** where a careless one would collapse them: a board, a board that is OFF (204 → `null`), and a read we could not make (throws). A 200 whose shape moved, or rows that arrived but none of which parsed, **throw** rather than answering "nobody is on the island" — the repo's most repeated bug class, and the one this file exists to refuse. Failures are never cached, so an outage cannot outlive itself; an OFF board is cached, so a dark board is not re-asked every render. The card **unmounts rather than apologising**: no error string ever reaches a visitor. Board handles run through the SAME `normalizeXHandle` as the reading rather than §4.2's local `handleOf`, which strips but does not validate — one implementation, so a spoofed handle cannot enter through whichever surface forgot to check. Mutations ×4: an unreadable board reading as empty reds 1, the card apologising reds 4, a tie displacing an existing flame reds 1, caching a failure reds 4. `flamesClient.test.ts` (30) + `FlamesBoard.test.tsx` (11), 41/41. **Still open in G:** A's "Watch the arrival" film mount and the lobby's em-dash rewrite. `id="hall"` shipped with B2. |
| H the doors in color | NOT-STARTED | | `VenueDoors.tsx:27` / `:134` confirmed exactly. Deleting `dimmedFilter` reds `VenueDoors.test.tsx` (its `isDesaturated` helper asserts settled doors **are** desaturated) and orphans the luma pipeline `doorThumbLuma.test.ts` guards in lock-step with `scripts/generate-image-derivatives.mjs`. <br><br>✅ **DONE — and two of its three done-means were ALREADY TRUE.** The header scrim landed 2026-09-04 and the count has always been live (`openCount`, derived from the registry, with its singular/plural). H was one change: the grayscale.<br><br>A settled door wore `grayscale(1)` plus a per-image brightness multiplier, and the multiplier was good engineering — door thumbnails ran 0.270 to 0.841 mean grey, a 3.1× spread, so plain greying left some tiles reading as switched off. Good engineering in service of a bad idea: it turned a resident's own art into a switched-off tile on the venue's front door. **Every door is in colour.** What a door IS still reads from things that carry meaning — the LIVE / SETTLED / QUIET chip, the accent border and glow, the hover scale only a live door answers. The 25% opacity step stays: depth, not desaturation, and it lifts on hover and focus.<br><br>**The apparatus went with it.** With no greying to normalise, `writeDoorThumbLuma()`, `doorThumbLuma.generated.json` and its test were dead weight still claiming a purpose — all three removed, full `npm run build` verified after. The hall's own sentence changed with the code, because it would otherwise be a lie ("greyed" → "marked"). **The guard flipped rather than being deleted, and it survived the ruling it was written to pin being REVERSED** — which is the argument for asking about the RESULT, not the mechanism. Seen red with grayscale re-added.|
| I em-dash zero, the guard | IN-PROGRESS — **guard landed, sweep underway** | `e2e/em-dash-zero.spec.ts` (50, seen red both directions) | **All 16 claims CONFIRMED — and the element collides head-on with this repo's degraded-read law.** The em dash is the venue's *unreadable* placeholder: `usePoolMarket.ts:13` states the rule outright, "render it as `—`, never as 0", and `LiquidityTab.tsx:391` emits it when a read fails. A body-wide "zero U+2014" assertion therefore **goes red on an RPC outage** — it converts a degraded read into a copy regression, in direct tension with "unreadable must not read as fine". Also: the directive's eight-file census omits `HomePage.tsx`, which carries 67 occurrences — a ~40% undercount. **RULED 2026-09-06 (owner): option (a)** — the rendered assertion is scoped to venue-voice text nodes and leaves degraded-read placeholders alone. No honesty behaviour changes and no existing test moves. Unblocked; the census must be re-sized to include `HomePage.tsx`. <br><br>📏 **THE COUNT, MEASURED — AND THE DISCRIMINATOR TURNS OUT TO BE STRUCTURAL, NOT EDITORIAL.** Walked nine venue-voice routes in Chromium, reading every TEXT NODE rather than the page's innerText, and separating two things a body-wide count cannot tell apart: **116 prose em dashes** and **7 standalone placeholders** (a text node whose trimmed content is exactly `—`, which is this repo's *unreadable* idiom and the thing the element must never touch). Per route: `/` **0** · `/faq` 0 · `/island` 2 · `/swap` 2 · `/liquidity` 6 · `/trust` 7 · `/nft-finance` 10 · `/launch` 32 · `/gallery` 82.<br><br>This dissolves the collision this row has been blocked on. The guard does not need to reason about copy at all: **flag a text node that contains U+2014 and whose trimmed content is not exactly U+2014**. `LiquidityTab.tsx`'s failed read and `usePoolMarket.ts`'s rule both emit the standalone form and stay green by construction, rather than by an exemption list somebody has to maintain. **And the venue arrival is already at zero** — element C's cut took it there, so the front door needs no copy edit at all. The work is `/gallery` and `/launch`, which are 114 of the 116.<br><br>✅ **THE GUARD LANDED, AND THE DISCRIMINATOR IS ITS WHOLE MECHANISM.** A text node containing U+2014 whose trimmed content is not exactly U+2014 is prose and fails; a node that IS exactly U+2014 is the unreadable placeholder and passes by construction. No exemption list, no copy review, and every future `{cond ? value : '—'}` is safe the day it is written. Text nodes, never `innerText` — `innerText` flattens a subtree into one string and glues a placeholder to the prose beside it.<br><br>🔴 **THE COUNT IS 487 ACROSS 52 ROUTES, NOT 141 ACROSS 9.** Both this file's earlier 116 and the island's corrected 141 are the same nine routes — the ones we had both been walking by hand. The venue's own nav reaches **fifty-two**. Worst first: `/changelog` **104** · `/gallery` **82** · `/launch` **32** · `/yield` **21** · `/competitions` **18** · `/alerts` **17** · `/eth-curve` **15** · `/curve-launch` **14** · `/tax` **13** · `/risks` and `/copy-trading` **12** each. Six routes are already at zero: `/`, `/faq`, `/history`, `/start`, `/admin`, `/launch/:token`.<br><br>**IT COULD NOT LAND AS `toBe(0)`** without landing permanently red, and a gate that is always red is a gate people learn to ignore. It lands as this repo's own `knownViolations` idiom: an exact count per route, asserted BOTH WAYS — a new dash fails the route and a FIX fails it too, until the number comes down with it (`routes.ts`: "a list that can only be appended to is a list that stops being read"). At the six zeroes the assertion is the island's ruling exactly: fail on the first prose node, and print that node's text so the failure names the copy.<br><br>**Seen red both directions before it was kept.** An em dash added to the venue hero's line reds `/` with `SPAN: Held time counts here — always.`; a budget raised by one reds that route with "is DOWN to 7 from 8, lower the number".<br><br>**Two traps, both measured rather than assumed.** The shared wallet fixture seeds `tegridy-bungalow='toweli'`, which makes `Footer.tsx:180` render `— Toweli` as its own text node on EVERY route — this seeds `'venue'` instead. And `/terminal` `/chart` `/copy-trading` `/competitions` read GeckoTerminal browser-direct, which `routes.ts` warns is "exactly the shape that flakes an EQUALITY assertion"; the feed is aborted so the degraded branch is pinned. Aborted they read 3/2/12/18, the same as the unstubbed sweep — but **the ready branch's copy is not walked and is not claimed**, because I cannot make a third party answer on demand.<br><br>⏳ **Owed, and one of them is the island's to rule:** the sweep to zero (`/launch`'s 30-odd go with F's fold; `/gallery` is a copy pass of its own), and whether `/changelog`'s **104** is in scope at all — it is a generated record of commits rather than venue-voice copy, and it is 21% of the whole debt. It is IN the guard rather than silently excluded, so the number is visible while the island decides.<br><br>🔴 **CI CAUGHT THE GUARD JUDGING A PHONE BY A DESKTOP RULER**, which a local Chromium run could not. `browserName !== 'chromium'` does not exclude the `mobile-chrome` project: it is a Pixel 5, so its browserName is `'chromium'` too. At a phone width `/exposure` reads 2 where the desktop reads 1 — not different copy, a different set of components rendering. Matched on project NAME now. ⏳ **Not walked, and therefore not claimed:** copy only a narrow viewport renders — the BottomNav's four tabs and the hamburger drawer. A mobile baseline is its own list and this element has not asked for one.<br><br>🧹 **THE SWEEP HAS STARTED: 22 dashes gone, NINETEEN routes at zero.** `/island` `/farm` `/vesting` `/airdrop` `/exposure` `/chart` `/checkout` `/dashboard` `/deployer` `/scan` `/swap` `/zap` `/eth-curve/:token` joined `/` `/faq` `/history` `/start` `/admin` `/launch/:token`. A finished route stops carrying a budget and starts failing on the FIRST prose node, printing the copy that broke it.<br><br>**The guard did the bookkeeping**, which is the half of the knownViolations idiom people usually leave out: it reddened on every FIX and named the new number.<br><br>🔴 **AND IT CAUGHT SOMETHING A SOURCE SWEEP WOULD HAVE MISSED.** `/dashboard`'s dash was written `&mdash;`, so every grep for the literal character missed it while the guard saw it at once — it reads RENDERED text nodes, and the browser has already decoded the entity. **18 files in `src/` use the entity form.** This is the argument for walking the page rather than the source: a source sweep would have declared `/dashboard` clean and been wrong.<br><br>Every rewrite follows your rule — shorter than what it replaced, a period, a comma or a colon, never two clauses for one dash. Two were parentheses wearing dashes ("Nobody — not the factory owner, not a guardian, not us — can…" → real parentheses); two were a dash carrying a list a colon carries better; the rest were a dash standing in for a full stop.<br><br>⚠️ **`/terms` is deliberately NOT swept.** Its three dashes sit inside fee and liability language, and rewriting the legal document is not a copy pass. ⏳ Remaining: `/changelog` **104** (awaiting your scope ruling), `/gallery` **82**, `/launch` **31**, and the mid-sized routes.|
| J the status file | IN-PROGRESS | `5f77da9e` opened it; amended per phase close | Opened at base `8b6144ae`. Two shape divergences from waves three and five, both deliberate and both flagged: `RETIRED` is a **new** fifth status word (both prior files document four), and §7 says "one row per element A to L" while the directive defines **A to P** — rows are written for all sixteen, since M/N/O/P have done-means of their own. |
| K the published law | **DONE** | `islandClaims.test.ts` (13, mutation-verified ×4) |The venue taught the SIZE TERM as the whole formula. It now teaches the island's published law, `heat = weight x (size + loyalty)`, with each term in the island's own words: size is the share curve, loyalty is held days and nothing else and anchors to your first hold, weight is the published multiplier (Apes triple, JBM and BAYLA their edge, the home team warm). **RETIRED:** the what-the-curve-pays table and the single-token-share-for-Observer sentence (under weight and loyalty neither is anybody's number), and the caps-at-100 / unreachable-on-a-single-position claims in `heatOracle.ts`. The "period has not been published" sentence is replaced by the island's own whole-held-time grammar, which it HAS published. **A THIRD SITE the directive did not name, and it was the rendered one:** `VENUE.heatPlain` told every visitor on the venue arrival "Each token you hold scores 0 to 100 degrees" — the same size-term-as-whole-law error, on screen rather than in a comment. Same shape as W4-02. The window and decay guards still pass untouched (7/7 within the 13). Mutations x4: the cap returning to a rendered surface reds 1, the unpublished-period sentence reds 1, the table returning reds 1 — and **deleting an entire term's explanation SURVIVED** until the guard stopped asserting the word and started asserting the island's definition of it. Third vacuous guard this wave, same root cause each time. **Unblocked** — the island's `/heat` page is up and its sentences are quoted above. Retires `HeatCard.tsx:446-520`'s computed table and single-token-share sentence, and `heatOracle.ts:1-21`'s "one token caps at 100°". The no-N-day-window and no-decay guards stay exactly as written. Note: the oracle's `HeatBreakdownRow` carries no weight or loyalty term, so no **per-token** breakdown of the law can be painted — only the served figure and the island's words.|
| L the phone wallet class | **BLOCKED** | | **Needs a phone in the operator's hand, once.** `Object.keys(window)` inside Trust Wallet's in-app browser, plus `window.solana?.isTrust`, `window.trustwallet?.solana`, and whether the Standard registry lists anything. Which of the two fixes applies is undecidable until then. If the legacy-adapter branch wins there is nothing in the repo to build on — `package.json:35-38` carries no Trust adapter — making it materially larger than the deep-link branch. Only Phantom's link shape is verified in-repo (`phantomMobile.ts:28`); the MetaMask and Trust formats appear nowhere here and are **unverified against vendor docs**. Trust's `coin_id` splits 501/60 by chain, so the shared "On a phone?" row needs a chain prop. |
| M the read link and the card | **DONE — AND WALKED ON PRODUCTION** | `2dc2fa39` · `middleware.test.js` (17, mutation-verified ×3) · `HeatCard.test.tsx` (30) · **X card validator PASS** |✅ **Every done-means closed on the live host, 2026-09-06.** `GET /read/0xd71caf9f…` as Twitterbot answers 200 with `og:title` = **"Elder · 1,694 days held · 1787.6° on Jungle Bay Island"**, `og:url` the read link, `twitter:card` `summary_large_image`, `X-Robots-Tag: noindex`, `Cache-Control: s-maxage=300, stale-while-revalidate=600`. A COLD wallet and a malformed address both answer the generic card, never a zero. **X's own card validator: `Page fetched successfully · 14 metatags were found · twitter:card = summary_large_image tag found · Card loaded successfully`** — the §6 requirement to walk it with the validator and not only curl, discharged. One departure recorded: a bad address answers the generic card at 200 rather than the specced 400, because a 400 shows the reader NO card where the generic one still says what the link is, and the human behind the same URL meets the instrument's own invalid state.<br><br> Built at the EDGE in `middleware.js`, not as an aggregator branch, and that is a deliberate departure with three reasons. (1) The directive's own design 403s its readers: reusing `heat.js`'s origin gate would refuse a human arriving from a tweet (no `Origin`, `Sec-Fetch-Site: cross-site`) while admitting the crawler. The edge has no such gate. (2) `middleware.js` already serves per-address unfurl cards for `/scan` and `/deployer` by exactly this pattern, intercepting **unfurl bots only** and letting humans fall through to the SPA, so no meta-refresh hop exists at all. (3) Zero serverless functions added, against a 11/12 budget. **No `vercel.json` rewrite was needed either** — the catch-all `/((?!api/).*)` → `index.html` already serves `/read/<address>`, and middleware runs before rewrites, so a bot gets the card and a human gets the app. og:title carries tier, days and degrees; days are the island's measured span, never our clock; `summary_large_image`; `noindex` (≈10^47 addresses answer 200 here, so it is a share surface, not a crawl surface); `s-maxage=300`. **A cold wallet and a failed read BOTH fall back to the generic card** — neither is "0°". og:image is the venue's static card until the island publishes its painted one. Human path: `/read/:address` → `/?heat=<address>`, so `?heat=` is the ONE hydration path and a shared link cannot drift from a pasted one. `middleware.js` had NO test before this; 17 now. Mutations: dropping og:title reds 1 (the directive's own named mutation), printing a zero on a cold wallet reds 1, serving the stub to humans reds 1. **The card rule at `middleware.js:15-20` was amended in the same commit** rather than left contradicting itself: it forbids asserting a verdict *we* compute, and a heat tier is the island's number quoted, not ours asserted. The route ships on its own static-og fallback; its **headline feature cannot** — the island's painted per-wallet card URL does not exist yet (**needed from the island**). One real bug to design around: reusing `heat.js`'s origin gate verbatim would **403 the human clicking from a tweet** (no Origin, `Sec-Fetch-Site: cross-site`) while passing the crawler. Done-means must test a cross-site click, not only curl. Also unbudgeted: `handleHeat` writes the HTTP response on every branch, so "read through the existing path" needs a value-returning extraction under a 500-line test suite.|
| N the named tape | **DONE** | `api/_lib/__tests__/tape.test.js` (16) · `tapeNames.test.ts` (12) · 5 mutations seen red | Upstream `x_handle` **confirmed served**. Two collisions: the proxy caps at `limit: 20/60s per IP` (`heat.js:106-110`), so twelve rows spend 12 of 20 and a second room 429s inside the minute; and raising the cache header 60/120 → 300/600 **reds `heat.test.js:508-511`** and overturns `heat.js:168-172`'s written rationale for the short window. `poolTrades.ts:29` allows `wallet: null`, so null rows must be skipped before the limiter or they burn calls on an empty string.<br><br>✅ **RULED (answer two §1.7): the tape gets its OWN door.** Neither the per-IP budget nor the twelve-row cap moves; both collisions above were signs the tape was using the wrong resource. New `?resource=tape&addresses=<a,b,c>`: at most twelve, validated with the same two regexes, above `const provider`, lazy import. **One venue request per room**, server-side fan-out to the island at **concurrency four**, a per-address in-memory memo for the function's lifetime, its **own** rate bucket (like flames), its **own** `Cache-Control: s-maxage=300, stale-while-revalidate=600`. The response is a map of address → the allowlisted reading (`x_handle · tier · degrees · held_since_unix · is_cold`) and nothing else. **One address failing upstream is `null` for that address, never a 502 for the whole tape.** `heat.js` and its written rationale stay untouched. Done means: twelve rows issue ONE venue request; at most four upstream reads in flight (assert on a mocked fetch); one upstream failure yields null for that address and 200 for the map; the map carries no key outside the five. Break the fix: drop the concurrency cap and watch the in-flight assertion go red. <br><br>✅ **BOTH HALVES SHIPPED.** `?resource=tape` sits at `aggregator.js:424`, above the `const provider` read at `:438` — verified in place, because a resource call carries no provider and a branch below that line never runs.<br><br>**One request per tape, not one per row.** The oracle is CORS-locked so a browser cannot read it at all, and twelve reads from the page would spend the instrument's own 20/min bucket twelve at a time and 429 the next visitor's reading. That is the whole reason the tape has its own door: its own rate-limit identifier and a 300 s edge cache rather than heat's 60. Four in flight, twelve max, **de-duplicated BEFORE the clamp** so one buyer taking six rows still leaves room to name the other six.<br><br>🔒 **THE STRIP IS THE POINT OF THE SERVER FILE.** A heat envelope carries a stranger's ENTIRE per-token breakdown — every contract they hold and when they first held it. A tape row needs three fields. Forwarding the rest would publish a buyer's whole portfolio beside their trade, to anyone who opens a room. An ALLOWLIST, for `flames.js`'s reason: a denylist forwards whatever the island adds next, silently.<br><br>**FAILURE LEAVES THE ROW, on both sides of the wire.** A wallet that throws, times out, returns non-JSON or comes back truncated gets no name; the whole call failing returns an empty map. Either way the row keeps the address it always had — never a blank, and never "unnamed", because *we could not ask* and *this person has no name* are different facts and only one is true during an outage.<br><br>**A flame with no handle gets no name**, refused on both sides: painting a stranger's tier because we could read them is not the same as them putting their name on it.<br><br>🔴 **The tests found a real bug in my own proxy:** `readBoundedText` returns `{ text, truncated }`, not a string, so `JSON.parse` was parsing an object and every read failed silently.|
| O the first-buy post | **BLOCKED** — one structural, two questions | | ✅ **RULED (answer two §1.5): NO OVERLAY, EVER — and O is not a receipt.** The overlay law binds here too, so a receipt interrupting every swap was never the answer. O is **one inline line in the swap's existing success state** on `TradePage` and the Solana swap page: after a confirmed buy whose output token is the room's contract, the success state prints "Post it: My clock on `<SYMBOL>` started today." beside the existing "view tx". The success state's type gains `tokenAddress`, `chainId` and `buyer`; the classic TOWELI receipt keeps its own share. **This dissolves the blocker rather than solving it** — the finding below was right that no swap raises `TransactionReceipt`, and the island's answer is that none should.<br><br>⚠️ **Verified at trunk, and the new scope is wrong in three places. Two are wording; one is a real defect and needs a ruling.**<br>1. *"The success state's type gains `tokenAddress`, `chainId` and `buyer`"* — **there is no success-state type.** `TradePage.tsx:699-703` is a bare JSX conditional over `swap.isSuccess && swap.txHash`. `ReceiptData` is the OVERLAY's type, reachable only from `FarmPage`, which this same ruling excludes. On TradePage two of the three values are already local (`chainId:93`, `swap.toToken`); only the buyer is missing, and that is one word at `:92` (`const { isConnected, address } = useAccount()`).<br>2. **The Solana page has NO inline success state at all.** `SolanaSwapPage.tsx` ends `handleSwap` in toasts (`:1355`, `:1357`) and clears the panel (`:1367-1368`). O there is a new latched state plus a new render block, not a one-line addition. Scope it accordingly.<br>3. 🔴 **THE EVM LINE WOULD VANISH MID-READ.** `useSwap.ts:314` runs `setTimeout(() => { reset(); … }, 4000)`, where `reset` is `useWriteContract`'s and clears the `hash` returned as `txHash` (`:651`) — so the whole success block self-destructs about four seconds after confirmation. A share prompt that disappears while somebody is reading it is a defect, not a nicety.<br>✅ **RULED (answer three §0): LATCH THE LINE, NOT THE STATE — neither option offered.** Lifting the timeout changes swap behaviour for every swap on the venue to serve one line; latching the whole success state does the same with a stuck form. **The element does not need the swap's state at all; it needs its own.** On a confirmed buy whose output token is the room's contract, write one latch `lastBuy = { hash, symbol, tokenAddress, chainId, buyer, atUnix }` into page state **and** sessionStorage under the room's key, so a reload inside the room keeps it. `useSwap.ts:314` keeps its `reset()` and its 4 s; the form clears for the next swap exactly as today; the toast lives and dies as today. **The line renders from `lastBuy`, never from `swap.isSuccess`**, and carries no timer: it stays until the holder taps Post (it becomes "Posted." and stays), submits another swap (latch replaced or cleared), or leaves the room (the latch is room-scoped and does not follow them). *"The seconds after a buy are the commitment moment; the line is what the venue says in those seconds, and it does not blink."* Copy branches on element D's scoped read: warm before the buy → "keeps running"; **a FAILED read prints "started today" rather than nothing, because here the line IS the element.** Solana builds the same latch and the same line on its confirmed-buy path and nothing else — no toast added, no form behaviour changed, no success block invented for symmetry. Done means: a fixture buy sets the latch and renders the line; **advancing timers 10 s leaves the line rendered while the form has reset**; Post opens the intent and the line reads "Posted."; a second submit clears it; another room renders none; a non-room token sets no latch. Break the fix: derive the line from `swap.isSuccess` and watch the 10 s assertion go red.<br>Also missing: **no by-address room lookup exists** in `bungalows.ts` (the registry has `address`/`chain`/`symbol` at `:65-68` but exports no finder), and it must skip undefined addresses and never match the native-ETH pseudo-address when `toToken.isNative`.<br><br>Original blocker, kept because it is why the scope changed: **The trigger does not exist.** No swap anywhere raises `TransactionReceipt`; the only production `showReceipt` calls are `FarmPage.tsx:301/338/348`, all `token:'TOWELI'`. Reads as a one-string change; is actually a new swap-receipt callsite on both `TradePage` and `SolanaSwapPage`. `ReceiptData` carries symbol **strings** only — no token address, no chain, no buyer — so the room-match is not expressible today. And the headline fixture is BAYLA, which is Solana, while the overlay is EVM-only (`sanitizeTxHash` rejects base58; no solscan branch). Depends on M for a link that 404s until M ships. <br><br>🔴 **THERE IS NO RECEIPT TO ATTACH A POST TO.** §O says "a bungalow buy's receipt offers the clock post". No swap path raises one: the `TransactionReceipt` overlay exists, but its only production callers are `FarmPage.tsx:301/338/348`, all `token:'TOWELI'`. The EVM swap's entire success state is a three-line JSX conditional at `TradePage.tsx:699-703`, and the Solana swap has no inline success surface at all. **O is not "add a button to the receipt" — it is "build the receipt"**, which is a different size of job and one the island should size deliberately rather than discover.<br><br>❓ **A COPY CONFLICT WITH YOUR OWN RULING.** §O hands one verbatim string: "My clock on &lt;SYMBOL&gt; started today." The ruling recorded in row D of this file mandates TWO branches — warm before the buy → "keeps running", cold or failed → "started today". Both cannot govern. Which does?<br><br>❓ **IF THE WARM BRANCH GOVERNS, IT NEEDS A READ NO SWAP SURFACE PERFORMS.** The scoped read is mounted only on a room's page, never on `/swap` or `/solana`, and `heatClient`'s cache is module-private with no synchronous peek. "Warm before the buy" therefore means a NEW `fetchHeat(buyer)` fired from the swap page at the moment of confirmation — a network call added to the buy path. A scope decision, not a detail.<br><br>⚠️ **WORTH KNOWING BEFORE RULING: six of the twelve live rooms can never show this line.** `/swap` is Ethereum-mainnet only, so the five Base rooms (QR, MFER, BNKR, DRB, JBM) have no in-venue buy at all — their trade route is a Dexscreener CHART. TOWELI is excluded by the directive itself. That leaves PEPE on the EVM rail and the five Solana rooms. Is that the intended reach?|
| P the planter's flame | **BLOCKED** — buildable, but fixture-only and three states short | | ✅ **RULED (answer two §2): P narrows to the venue's OWN Memetics Curve rail**, which exposes a creator on chain where the Doppler ledger cannot. Verified at trunk, and the island's three anchors hold with **two drifts worth carrying**: the files are `src/lib/launcher/curve.ts` and `src/lib/launcher/curveIdentity.ts` (not `src/lib/`), and **the event is `LaunchCreated`, NOT `Launched`** — there is no event named `Launched` anywhere in `frontend/`, so a `parseEventLogs`/`watchContractEvent` written to the directive's spelling would silently match nothing forever. `creator: Address` is at `curve.ts:46`, backed by the real ABI tuple at `:276`, and indexed as topic2 at `:314-316`. `curveIdentity.ts:224-230` already reads it as the **spoof defence** (the `owners` filter), so it is load-bearing, not decoration. **Render targets, both already holding the creator in scope so no new chain read is needed:** `CurveLaunchesGrid.tsx:219` (`useCurveIdentity(token, chainId, launch?.creator)`) — `CurveGridCardData` (`:52-63`) must gain the field to paint it; and `CurveTokenPage.tsx:216` (`resolved.creator`, already used at `:350` and `:380`). Note the curve token header is SPLIT: page chrome at `CurveTokenPage.tsx:269-282`, per-token identity at `CurveTradePanel.tsx:95 IdentityHeader`. The Doppler ledger prints **no planter line and never a placeholder** — its two producers were re-confirmed to hardcode `creator: null` structurally, not as a TODO (`discovery.ts:205`, `ourLaunches.ts:127`, whose `AssetRecord` has no creator field to read).<br><br>Original blocker, kept because it is why the scope moved: **The creator EOA is `null` in both producers** — `discovery.ts:205` ("not exposed by `new_pools`") and `ourLaunches.ts:127`, which states the Airlock record exposes only `timelock`/`governance`, "neither of which is the creator EOA". With no creator, "a failed read prints nothing" collapses to **prints nothing, always**: the feature would be invisible in production while passing every fixture test. A creator source is a prerequisite, not a detail. Adding it to `LaunchFactSheet` moves the disclosures digest and is a `schemaVersion` call. Both card surfaces are also declared **fetch-free by contract**, so reads must lift to the parent and de-dupe by creator wallet. <br><br>✅ **YOUR `LaunchCreated` CORRECTION HOLDS, AND IS STRONGER THAN STATED.** There is no `event Launched` in the frontend OR in the Solidity — `TegridyCurveLauncher.sol:168` declares `LaunchCreated`, and the single place the app names a curve event (`CurveCreatePanel.tsx:433`) already uses it. Better still: **P needs no event work at all.** The grid discovers launches from the on-chain INDEX (`launchCount` + `allLaunchesPaginated`), not from logs, and `launch.creator` is ALREADY in scope and unused at all three render targets. P costs zero new chain reads.<br><br>🔴 **BUT NOTHING RENDERS IT, ANYWHERE.** Read live rather than inferred: `launchCount()` returns **0** on BOTH deployed curve launchers — Base `0xa517A1cE…` and Robinhood `0xA2e7E7Fa…`, both carrying real bytecode. Zero launch cards and zero curve token pages with data exist in production. **P is fixture-only testable**, and every "it works" claim for it would rest on fixtures alone. That belongs in the row before it is built, not after.<br><br>❓ **THE DONE-MEANS COVERS TWO OF FIVE STATES.** "handle, tier and days, or the unnamed form" does not say what a card renders when: **(a)** the planter is COLD — element B renders NEITHER form for a cold wallet, the whole block is gated on `!isCold`; **(b)** the instrument is unreachable — and the venue's law is that an outage never reads as a zero or as "unnamed"; **(c)** days is null because a timestamp is missing.<br><br>❓ **WHICH UNNAMED FORM IS CANONICAL?** B says "No name on this flame yet. Put yours on it"; G's board says "No name yet. Yours?". The done-means says "the unnamed form", singular. P is address-driven like B, so B's is the better match — but that should be said, not assumed.<br><br>⚠️ One note either way: `daysHeld` is module-private in `HeatCard.tsx:64`, and `heatOracle.ts:396-400` deleted the shared day-counter on purpose and forbids re-adding it there. The shared helper needs a home before P can render a day count.|
| W4-02 the ninety-day grammar | **DONE** | this commit · `heatOracle.ts` + `BirthQueuePanel.tsx:67` | Answer one supplied the grammar verbatim ("Arrivals prove ninety days. Births don't.") and named ONE site to fix. **There were two.** The comment at `heatOracle.ts` was there as described. But `BirthQueuePanel.tsx:67` is rendered JSX, not a comment, and told every reader of the birth-queue panel that Heat is measured from birth "rather than after a half-year wait" — so answer one's "no rendered venue surface states the old figure any more" was false, and the site it missed is the one a person actually reads. Both corrected. Full record in `docs/ISLAND_WAVE_FOUR_STATUS.md`. |
| W4-03 the published law | **DONE** | via element K, this commit | Closed through element K. Wave four's full phase list now lives in `docs/ISLAND_WAVE_FOUR_STATUS.md`. |
| 16 the art runbook | NOT-STARTED | | New in answer one, carried from wave six G, and **deliberately last: infra and UI first**. `docs/ISLAND_ART_RUNBOOK.md`, five steps, plain words, zero em dashes: per-bungalow folders at the repo root · `scripts/gen-bungalow-art.mjs` · ids from FILENAMES, never from array index (the repo's own art law — overrides key on artId, and an index-keyed override silently repaints the wrong piece) · placement in the live per-door studios · export DMed back. The QR has no folder. The art itself is the owner's lane and nothing in this wave waits on it. |

## Decisions

Flagged, not forced, per §8. The first three were put to the owner on 2026-09-06 and ruled;
the rest stay open and nothing that depends on them is touched.

### Ruled 2026-09-06

1. **Element I's em-dash guard vs. the degraded-read law — RULED: scope to venue-voice.**
   The two could not both hold as written: the em dash is this venue's *unreadable*
   placeholder, so a body-wide assertion turns an RPC outage into a copy regression. The
   rendered leg now walks venue-voice text nodes only. It is the one option that changes no
   honesty behaviour and moves no existing test. Element I is unblocked.
2. **Element A's art cut — RULED: A ships only with G's film mount.** `T_ART_COUNT` 4→1
   removes three gallery pieces from the arrival, and the repo never removes art without an
   additive home. The "Watch the arrival" full-film mount is pulled forward from element G
   into A's own change, so the cut and the new home land together or neither does.
3. **Git handling — RULED: feature branch, commit as you go.** The wave is built on
   `feat/island-wave-seven` off `mvp-launch`, one commit per element close with this file
   amended in the same commit (§7). Nothing reaches trunk without a PR.

### Ruled 2026-09-06 by the island (answer two §1)

**All seven are now closed.** The island concurred with the operator on 1–3 and ruled 4–7.

4. **The replay easter egg — RULED: RETIRE it** in favour of G's "Watch the arrival" link.
   The island's reason: *"One door for the film. Two doors to the same room is the kind of
   thing this wave exists to remove."* So element A deletes it rather than repointing it at
   localStorage, and `TopNav.tsx`'s handler goes with it.
5. **Element O — RULED: NO OVERLAY, EVER.** The overlay law binds here too: nothing opens
   over the page unasked, and a receipt interrupting every swap is exactly that. **O is not a
   receipt.** It is ONE INLINE LINE in the swap's existing success state on `TradePage` and
   the Solana swap page: after a confirmed buy whose output token is the room's contract, the
   success state prints *"Post it: My clock on `<SYMBOL>` started today."* beside the existing
   "view tx". The success state's type gains `tokenAddress`, `chainId` and `buyer` so the line
   can be built; nothing else about the swap changes, and the classic TOWELI receipt keeps its
   own share. **This retires the scope that made O BLOCKED** — the missing piece was never a
   receipt callsite, it was the wrong shape.
6. **Element M — RULED: the edge stands; the aggregator branch is retired from the directive.**
   *"Two mechanisms for one job is a defect."* The island's stated grounds are the ones this
   file gave: it already existed, adds zero functions, refuses nothing a tweet sends, and
   carries `noindex`. The rule text at `middleware.js` is **amended, not contradicted** — one
   law now, "a card asserts nothing OF THE VENUE'S OWN", under which a served island tier
   printed verbatim is quoting rather than asserting. Done in this commit.
7. **Element N's rate limit — RULED: neither budget moves. The tape gets its own door.**
   Twelve reads through `?resource=heat` spending 12 of 20/min, and raising `heat.js`'s cache
   header, *"are both signs the tape is using the wrong door"*. See row N for the new
   `?resource=tape` contract. `heat.js` and its written rationale stay untouched.

One line the island added to decision 1: the unreadable placeholder glyph **may live in a data
cell and never inside a sentence** — a dash alone in a cell where a read did not land is a data
glyph, not prose. An optional systemic alternative was offered and explicitly **not ordered**:
render the placeholder as U+2013 and the body-wide guard needs no exemption class at all.

## Needed from the island

**All four session-one asks were answered on 2026-09-06 (answer one).** Recorded closed:

1. ~~Wave six's phase list~~ → supplied, seven elements, opened as
   `docs/ISLAND_WAVE_SIX_STATUS.md`. Six RETIRED into wave seven; **row C is not**, because
   the tab it concerns is not gated off (see that file).
2. ~~Wave four's numbering~~ → supplied, six phases, opened as
   `docs/ISLAND_WAVE_FOUR_STATUS.md`. This also closes the ask standing since
   `ISLAND_WAVE_FIVE_STATUS.md:147-150`.
3. ~~The painted card URL~~ → answered, and the answer is "not yet". The card is a PAGE at
   `memetics.wtf/card/<wallet>`, live but **not fired** (the owner's two taps in the island's
   growth lane, not ours). The IMAGE url behind its `og:image` is unpublished and will be
   handed over exact in a later status exchange. Element M's `og:image` stays the venue's
   static card, which is what it already does. **Explicitly forbidden and not done:** do not
   scrape the card page for its image URL, and do not point `og:image` at a page.
4. ~~`/api/flames?token=`~~ → answered: **does not exist tonight.** Nothing to read and
   nothing to mock against a guess. No room paints a per-token board; element D's "Your held
   time in `<SYMBOL>`" is the per-token read that exists, and it is enough for this wave.

**Still open, and now the only island-side blockers:**

- The card IMAGE url, once the card is fired (element M's headline feature).
- `/api/flames?token=<address>`, if a per-token board is ever wanted (element N/D).

## Sent back to the island with this commit (answer two's own claims, verified)

Answer two owned both of session one's corrections without argument. Verifying it produced
**four more**, all read at trunk rather than inferred:

1. 🔴 **The curve event is `LaunchCreated`, not `Launched`** (`curve.ts:314-316`). There is no
   event named `Launched` anywhere in `frontend/`. A reader who took §2's spelling literally
   would write `eventName: 'Launched'` and match **nothing, silently, forever**. This is the
   most costly of the four because it fails quiet.
2. **§2's file paths drop a directory**: `src/lib/launcher/curve.ts` and
   `src/lib/launcher/curveIdentity.ts`. The line numbers are exact.
3. **§1.5's "the success state's type gains…" has no referent.** `TradePage`'s success state
   is a bare JSX conditional with no type; the only `ReceiptData` belongs to the overlay the
   same ruling excludes. And the Solana page has **no success state at all**, so element O is
   a new latched state there, not a one-line addition.
4. 🔴 **The EVM success block self-destructs after 4 s** (`useSwap.ts:314` → `reset()` clears
   the `hash` returned as `txHash`). A first-buy share prompt that vanishes mid-read defeats
   element O's entire purpose. **Needs a ruling: lift the timeout, or latch the success state
   independently.** Flagged rather than decided, because it changes swap behaviour.

And one correction this file owes **itself**: the `tf_loaded` count was seven; it is nine
statements across six files. See row A.

### Answer three: all four owned, and one of them changed the island's own process

Every correction above was taken. The `LaunchCreated` one is worth recording in full,
because the island named the cause rather than just the fix:

> "The island grepped the creator field at `curve.ts:316` and named the event from memory. A
> reader who matched 'Launched' would match nothing, silently, forever, exactly as you said.
> **The island's own law paid it: a grep is not a read.** Every event name in a payload is now
> read from the ABI line that declares it."

That is the same law this file has been applying from the first pass, arriving from the other
direction. Six corrections have now travelled between the venue and the island across three
answers; **none was argued by either side**, and two of them changed a process rather than a
line. The island also confirms at its own bar: session one is on trunk, and **row M is DONE**
— *"the island's reference wallet unfurls as 'Elder · 1,694 days held · 1787.6° on Jungle Bay
Island'. That is the first artifact of held time to leave the venue into a feed."*

The nav walk stays an **order**, not a note: walk `/nft-finance` live, read the boundary and
the console, then fix the class or set `PROMOTE_PENDING` false. Wave six row C is open until
one of those two is the evidence.

## The nav walk, ordered rather than noted

`/nft-finance` — **SUBSTANTIVE, and reachable.** Walked cold in Chromium, whole rendered
text after a scroll to the bottom. It is not a promoted empty page: three panels read **LIVE**
(NFT Lending, NFT AMM, Launchpad) and three read **SOON** (Token Lending, Pooled Lending, Pay in
Instalments), each labelled at the point a visitor would act on it. The labels are true against
the addresses: `TEGRIDY_NFT_LENDING_ADDRESS`, `TEGRIDY_NFT_POOL_FACTORY_ADDRESS` and
`TEGRIDY_LAUNCHPAD_V2_ADDRESS` are all non-zero in `constants.ts`, and `TEGRIDY_LENDING_ADDRESS`
is the one that reads `0x0000…` — which is exactly the panel that says SOON.

**One thing the walk cannot settle from here**, and it is this repo's own standing rule:
REGISTERED is not DEPLOYED and neither is WIRED. A non-zero constant proves an address was
recorded, not that the contract is live and wired on chain. The honest answer to
"substantive or hidden" is **substantive**; the answer to "and is every LIVE label earned" needs
an on-chain read of those three, which is not a nav question and is not claimed here.

It reaches a visitor through Earn → NFT Loans rather than the top bar, which is the
2026-09-05 tabbed-host arrangement and not a wave-seven change.

## Outside the wave, and it was blocking everything

**THE ARRIVAL HAS A NEW BACKDROP (owner pick, 2026-09-09).** `public/art/door-home.jpg`
replaces `iphone/IMG_0148.jpg` as the venue arrival's full-page art, which is also the LCP
image. Four sites, because this surface is PINNED rather than rotated: the file, the `ART`
entry, `artOverrides.ts`'s `"home:0"`, and `theme-init.js`'s `HERO_SRC` (the `<link
rel=preload>` the shell emits). `heroPreload.test.ts` pins the last two against each other,
so missing the fourth fails the build rather than shipping a preload for a picture nobody
sees. Deliberately NOT added to `ART_POOL_ALL`: that array's LENGTH feeds the deterministic
hash that assigns art to every un-overridden surface, so one entry would silently reshuffle
backgrounds app-wide. `iph_0148` stays in `ART` and in the pool; this repoints a surface, it
does not retire a piece. **The crop was picked by eye at three widths, not by arithmetic** —
the first value centred on the subject and put the headline straight on the ape's face at
390 px and 820 px, because this image is 1.78 wide where the one it replaces was 1.41 and
`object-cover` therefore zooms much harder on a tall screen. `20% 50%` instead, which is the
answer the old art had already reached from the other direction (`4% 85%`, pin the quiet
corner).

**🔴 AND THE FIRST TWO ATTEMPTS AT IT WERE BOTH WRONG, BOTH CAUGHT BY THE OWNER, AND
BOTH WORTH RECORDING.**

*It repainted a room it had no business in.* `home:0` is not the venue's surface — it is the
SHARED classic one, drawn by the venue arrival, by the TOWELI room, and by anything else
resolving the classic pool. Repointing it gave /toweli a new backdrop too. The venue reads
`venue-home:0` now; `home:0` is byte-identical to what it was (`iph_0148` at `4% 85%`), which
is asserted rather than eyeballed: the whole diff of `artOverrides.ts` against trunk is one
ADDED line. `heroPreload.test.ts` gains the half that would have caught it in the first place
— an assertion that the two surfaces are NOT the same picture, so pointing the venue back at
`home:0` reds instead of quietly moving a room.

*And the crop was tuned for the wrong thing.* I pinned it to the quiet dark corner (`20%`) to
protect headline legibility, and on a phone that pushed the ape off the right edge entirely: a
picture of a door with a shoulder in it. The owner's own screenshot is what showed it. Swept
34 / 38 / 42 / 46 / 50 / 54% against real iPhone and iPad viewports and looked at all twelve
frames: **42%** centres him with the doorway behind him, whole, at both sizes, and costs the
desktop nothing (1440×900 has 159 px of horizontal slack in total). The lesson is the one
this wave keeps relearning in a new costume — **the arithmetic answer and the looked-at answer
were different, and the looked-at one was right.** Verified live on production at both sizes,
and /toweli verified live still on its own picture.

**AND CI WAS RED FOR A REASON THAT WAS NEVER THIS BRANCH.** `E2E Tests (Anvil fork — money
paths)` had been failing in 89 seconds — on trunk too, at the merge of #467, and it would
have failed on every PR opened from now on:

> `HTTP 403 — {"code":-32602,"message":"Archive requests require a personal token."}`

`ethereum-rpc.publicnode.com` gated ARCHIVE requests behind a token on 2026-09-09, and an
`anvil --fork-url` **is** an archive request. Not a rate limit, so waiting never cleared it.
The repo had already met the same policy on publicnode's INDEXED requests months ago
(`BungalowHolders.test.tsx:7` records it); this is that policy reaching archive.
**Reproduced locally against anvil 1.5.1 rather than inferred from a CI log** — publicnode
fails with the identical 403 on this machine, which is how it was proved to be the endpoint
and not a runner IP. The FORK moved to `https://eth.drpc.org`; plain `latest` reads still
answer on publicnode, so the seven other scripts pointing there are untouched. Proven end to
end through the wrapper rather than around it (`npx playwright test` skips the anvil leg and
would have looked fine): `[e2e] fork ready at block 25944402`, and
`stake → claim → unstake (Anvil only)` passing in 14.0 s. Two defaults have now died at that
line, so the note above it says what killed each and what to do next: if drpc rate-limits
under CI load the durable answer is a funded key in a repo secret, not a fourth free endpoint.

## Counts

**e2e, this tip:** `em-dash-zero` **50/50** · `launch-fold` **4/4** · `a11y-routes` + `heat-gate` **90/90** · `bungalow-doors` **24/24** · `arrival` **16/16** · `arrival-voice` **4/4** · `gauge-voting` **11/11**. Chromium, `--workers=1`.<br><br>⚠️ **RUN THESE FROM A PORT OF YOUR OWN.** `playwright.config.ts` hardcodes :4173 with `reuseExistingServer`, and this box runs several sessions out of several worktrees. Another session's `vite preview` owned the port and 24 tests failed against ITS build — element C read as missing, TOWELI read as back in every room, none of it real. The symptom is failures that all look like your feature was never applied.

**Loader vitest:** 58/58 across four files (`skip` 30, `curtain` 16 → decisions only, `curtainDeadline` 8, `index` 4). All four new guards were seen red on the pre-fix source before they were kept.


**vitest 5838/5838 — ZERO failures.** The 21 reds this file has been attributing to the concurrent session are gone: merging trunk into the branch brought in the exports `bungalowStakingCeiling.test.ts` was missing, so the pair that could not compile now passes. The branch has a completely clean baseline for the first time this wave.
`middleware.test.js`, on a quiet machine (`--maxWorkers=2 --testTimeout=30000`, 521 s).
Typecheck 0 across both projects (`tsc -b --force` and `tsc -p tsconfig.test.json`) for every
file this wave owns. eslint 0 errors on every file this wave has touched. Server-side: 774/774
across the heat and aggregator suites.

**The 3 reds are the other session's, and that is provable rather than assumed.** They are
`bungalowStakingRates.test.ts > lockPresets`, failing because "6 Months" and "1 Year" vanished
from the ladder. `git log mvp-launch..HEAD -- frontend/src/lib/bungalowStaking.ts
frontend/src/lib/bungalowStakingRates.test.ts` is **empty** — no wave-seven commit has ever
touched either file — and the uncommitted diff in `bungalowStaking.ts` introduces
`OFFERED_LOCK_CEILING_DAYS = 90` and `offeredMaxLockDays()`, which is exactly what removes
those two presets. That is the u64 claim-ceiling work in flight with its sibling test not yet
updated. Wave seven's own effective count is 5619/5619.

**One pre-existing red, not ours and not touched:** `src/lib/bungalowStakingCeiling.test.ts`
is UNTRACKED (no git history, present in the worktree before this wave opened) and fails at
import on five exports `bungalowStaking` does not have — `CLASSIC_ACCOUNTED_CEILING`,
`claimCeilingReached`, `anyClaimCeilingReached`, `maxSafeStakeRaw`, `maxSafeStakeAcrossPools`.
It is WIP for the Streamflow u64 claim ceiling and it fails identically with every wave-seven
commit reverted. Excluded from the count above; named here so nobody reads it as ours.

⚠️ **A SECOND SESSION IS EDITING THIS CHECKOUT.** From 2026-09-06 a parallel task (the
Streamflow u64 claim-ceiling guard, see the pre-existing red above) began editing
`frontend/src/lib/bungalowStaking.ts`, `frontend/src/components/bungalow/LighthousePoolLive.tsx`
and `frontend/scripts/bayla-lighthouse-ceremony.mjs` in this same working tree. Those files are
**not wave seven's** and are not staged by any wave-seven commit; each commit here stages an
explicit file list rather than `git add -A` for exactly this reason, and `wave7-session1-…` tags
the tip. A `tsc -b` run may therefore report errors in `LighthousePoolLive.tsx` that belong to
that work mid-edit. Check `git status` before reading any red as ours.

Per wave five's standing warning, **counts are read from a quiet machine only**: this checkout
is on OneDrive and loaded runs have reported up to 70 false failures, including pure arithmetic
that cannot regress from a copy change. A red from a loaded run is not evidence.

Not yet run this wave: the full e2e suite, the production build, and the rest of the §6 cold
walk (element L still needs a phone).

## Walked on a real deploy, 2026-09-06

Vercel's GitHub integration auto-built a preview from the pushed branch (deployment for
`50e0c97a`). **Walked in a browser against that deploy, not a dev server**, reading the live
island. What was seen, not inferred:

- **Element M's human path works.** `/read/0xd71caf9f…` redirected to `/?heat=0xd71caf9f…`
  and the instrument hydrated and read on load, with no click.
- **The read paints the island's order**, live: `ELDER` · `1,694 days held` · `1787.63°` ·
  `on the island since January 2022` · `18 tokens counted`, then `Reckoned 7h ago`, then
  `@_seacasa · On the board.`, then the 18-row breakdown summing to 1787.63°.
- **The insertion rank was correctly ABSENT** — this flame is named, so its real position is
  the island's to state. The suppression is not just unit-tested; it was observed.
- **The board rendered five named flames** with degrees, tier verbatim, since-month and token
  counts, both island doors under it, and `read Sep 6, 19:16 UTC`.
- **THE STRIP HOLDS IN PRODUCTION.** Neither `wallet_count` nor `person_id` appears anywhere
  in the rendered page. The unit test asserts it; the deploy confirms it.
- The three paths render with `The floor is 80°.`, and the hero is three lines with the field
  always open and one filled button.
- Both proxy calls were observed at the network layer and both answered **200**:
  `?resource=heat&address=…` and `?resource=flames&limit=5&claimed=1`.

**Element A's premise is confirmed live, and it is worse than the source read suggested.**
`get_page_text` returned the FULLY hydrated home — reading, board, paths, hall — while a
screenshot at the same instant showed only the arrival film. The app is mounted and complete
underneath an opaque `z-index: 9999` canvas for the full ~14.5 s. Nothing is waiting to
render; it is purely a visibility and interactivity wall, exactly as element A argues.

**Still not done, and it needs the operator:** the X card validator walk against
`/read/<address>`. The preview sits behind **Vercel SSO deployment protection** (a bot request
302s to `vercel.com/sso-api`), and X's crawler cannot authenticate. Turning protection off, or
issuing a Protection Bypass for Automation token, is an account setting and therefore the
operator's click, never Claude's. The same middleware path DID pass the validator on the live
`/scan` card the same day (`Page fetched successfully · 14 metatags · twitter:card =
summary_large_image · Card loaded successfully`), so the mechanism is proven; only this route's
title is unwalked.
