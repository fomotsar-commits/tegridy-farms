# v2-provenance expected diff — UniswapV2OracleLibrary
# ours     : contracts/src/lib/UniswapV2OracleLibrary.sol (identifiers renamed to upstream vocabulary; see TARGETS in scripts/check-v2-provenance.mjs)
# upstream : upstream/v2-periphery/UniswapV2OracleLibrary.sol (v2-core@6a9e7c978606, v2-periphery@ed2499130429)
# GENERATED by `node scripts/check-v2-provenance.mjs --update-snapshots`. NEVER hand-edit.
# Line numbers refer to the NORMALIZED one-statement-per-line streams, not source lines.
# Every hunk below is a pinned deliberate divergence from canonical Uniswap V2.
# The named allowlist (rationale + pinning test per divergence) is contracts/provenance/PROVENANCE.md.
@@ up:1 ours:1 @@ interface IUniswapV2Pair
- ==== FILE upstream/v2-periphery/UniswapV2OracleLibrary.sol ====
- import "@uniswap/v2-core/contracts/interfaces/IUniswapV2Pair.sol";
- import "@uniswap/lib/contracts/libraries/FixedPoint.sol";
+ interface IUniswapV2Pair {
+ function getReserves() external view returns (uint112 reserve0, uint112 reserve1, uint32 blockTimestampLast);
+ function price0CumulativeLast() external view returns (uint256);
+ function price1CumulativeLast() external view returns (uint256);
+ }
  library UniswapV2OracleLibrary {
- using FixedPoint for *;
+ function fraction(uint112 numerator, uint112 denominator) internal pure returns (uint224) {
+ require(denominator > 0, "FixedPoint: DIV_BY_ZERO");
+ return uint224((uint256(numerator) << 112) / denominator);
+ }
  function currentBlockTimestamp() internal view returns (uint32) {
  return uint32(block.timestamp % 2 ** 32);
@@ up:13 ours:18 @@ function currentCumulativePrices(address pair) internal view returns (uint256 price0Cumula
  (uint112 reserve0, uint112 reserve1, uint32 blockTimestampLast) = IUniswapV2Pair(pair).getReserves();
  if (blockTimestampLast != blockTimestamp) {
+ unchecked {
  uint32 timeElapsed = blockTimestamp - blockTimestampLast;
- price0Cumulative += uint256(FixedPoint.fraction(reserve1, reserve0)._x) * timeElapsed;
- price1Cumulative += uint256(FixedPoint.fraction(reserve0, reserve1)._x) * timeElapsed;
+ price0Cumulative += uint256(fraction(reserve1, reserve0)) * timeElapsed;
+ price1Cumulative += uint256(fraction(reserve0, reserve1)) * timeElapsed;
  }
  }
  }
+ }
