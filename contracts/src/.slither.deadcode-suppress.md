# Slither `dead-code` detector — re-enabled 2026-05-03

## Date
2026-05-03

## Reason for re-enable
Fresh-eyes audit found that excluding the `dead-code` detector was hiding
genuinely-unreachable functions (B7-class findings — unreachable
internal helpers, abandoned legacy entry points, and orphaned modifiers
left behind after refactors). The previous rationale ("Many helpers
retained for ABI compatibility and future use") was applied too
broadly: ABI-compatibility helpers are **public/external** and Slither
correctly does not flag those as dead. The detector only fires on
`internal`/`private` functions with no callers, which is exactly what we
want surfaced in a live audit.

## How to suppress per-line if a finding is intentional
Use the inline pragma directly above the function (or contract member)
that should be exempted:

```solidity
// slither-disable-next-line dead-code
function _abiKeptForFutureMigration(address user) internal view returns (uint256) {
    // Documented future-use helper — referenced from off-chain ABI codegen
    // tooling but not yet wired up in any on-chain call path. See B7-batch
    // ledger entry 2026-05-03 for justification.
    return _shadowState[user];
}
```

## When NOT to suppress
- The function is genuinely unreachable: **delete it**, do not suppress.
- The function is reachable only via `delegatecall` from a known proxy:
  add a `// slither-disable-next-line dead-code` AND a one-line comment
  pointing at the proxy.
- The function is reachable but Slither cannot infer the call (e.g.
  invocation built from `abi.encodeWithSelector` in another contract):
  prefer adding a unit test that hits the path; the test itself becomes
  the call site Slither can see.

## How to verify locally
```
cd contracts
slither . --config-file ../slither.config.json --detect dead-code
```

## Active suppressions

### `TimelockAdmin._executeAfterOf(bytes32)` (`contracts/src/base/TimelockAdmin.sol:215-217`)

**Status:** Suppressed via inline `// slither-disable-next-line dead-code` (added 2026-05-03).

**Rationale:** Documented in-source as the deprecated alias of the canonical `_proposalReadyAt(bytes32)` accessor. PASS6 slither triage (`.audit_101/PASS6_SLITHER_2026_05_03.md`, finding E22) confirmed:

- The canonical replacement (`_proposalReadyAt`) is wired and used at 5 in-tree call sites (CommunityGrants:658, CommunityGrants:711, TegridyFeeHook:331, TegridyTWAP:546, TegridyTWAP:575).
- `_executeAfterOf` has 0 in-tree callers but is preserved as a back-compat alias for any out-of-tree consumer that may have integrated against the older name (a real risk given the public ABI exposure of TimelockAdmin via the timelocked-admin contracts that inherit it).
- The original AUDIT v3-LIB-I1 work explicitly noted "kept as a back-compat alias only" with a planned removal at a major-version bump.

This matches the "ABI-compatibility helpers" carve-out for the back-compat case described above. The function will be deleted at the next major version once all downstream consumers (none in-tree today) have migrated. No action required.

### Pending — to be deleted in a follow-up cleanup task (NOT suppressed)

The PASS6 triage also identified two genuinely unreachable internal helpers that should be **deleted, not suppressed** per the guidance above. Listed here for visibility; out of scope for the PASS6 triage commit.

- `CommunityGrants._countActiveProposals()` (CommunityGrants.sol:914-916) — wrapper around `activeProposalCount`, no callers.
- `RevenueDistributor._getRestakedAmount(address)` (RevenueDistributor.sol:517-527) — orphaned after the C-1 / DR-04 voting-power refactor.
