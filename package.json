{
  "name": "tegriddy-farms-root",
  "private": true,
  "description": "Root devDependencies for repo-wide tooling (OG banner rasterizer, etc.). The actual sub-projects live in frontend/, contracts/, indexer/ — each with their own package.json.",
  "license": "MIT",
  "scripts": {
    "render-og-png": "node scripts/render-og-png.mjs",
    "extract-abis": "node frontend/scripts/extract-missing-abis.mjs",
    "check-selectors": "node scripts/check-interface-selectors.mjs",
    "check-v2-provenance": "node scripts/check-v2-provenance.mjs",
    "capture-airlock-selectors": "node frontend/scripts/capture-airlock-selectors.mjs",
    "diff-addresses": "npx tsx scripts/diff-addresses.ts",
    "predeploy": "node scripts/predeploy-check.mjs",
    "verify-ownership": "node scripts/verify-ownership.mjs",
    "oneshot-guard": "node scripts/oneshot-guard.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
