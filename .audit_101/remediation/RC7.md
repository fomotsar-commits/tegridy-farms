# RC7 — Recovery pass: lib + key-component fixes reverted

**Date:** 2026-04-26
**Type:** RECOVERY (re-applied work that was reverted in an earlier wave)
**Verification:** `cd frontend && npx tsc --noEmit` → exit 0 (clean).

## Files restored

| # | File | Source | Change |
|---|------|--------|--------|
| 1 | `frontend/src/components/nftfinance/LendingSection.tsx` | R011 | Typed `parsePosition()` accessor; `computeLTV(principal, positionEthValue:number)` consumes ETH-equivalent (TOWELI × `priceInEth`); `getRepaymentAmount` gains `refetchInterval: 12_000` + post-success refetch; countdown via shared `useCountdown(deadline)` hook |
| 2 | `frontend/src/components/nftfinance/NFTLendingSection.tsx` | R011 | Repayment refetch wiring (`refetchInterval: 12_000`, refetch on `repaySuccess`); `useCountdown(loan.deadline)` drives `deadlineStr`; `MyLoansTab` iteration `1..n → 0..n-1` (and `id: i+1 → id: i`) aligned with on-chain 0-indexed `loans[]` |
| 3 | `frontend/src/lib/aggregator.ts` | R045 | `chainId: number` required; `SUPPORTED_CHAIN_ID` rename; empty-result + zero-HTTP on chain mismatch; uniform `maxSlippagePct` (default 0.5%, clamped `[0.05, 50]`) flows to SwapAPI / Odos / Li.Fi; `chainId` + `maxSlippagePct` tagged on every `AggregatorQuote` |
| 4 | `frontend/src/lib/storage.ts` | R045 + R080 | `EVICTABLE_PREFIXES = ['tegridy_', 'tegridy-']` exported + `isEvictable(k)`; `safeJsonParse<T>(str, fallback): T` with explicit fallback signature |
| 5 | `frontend/src/lib/errorReporting.ts` | R046 + R057 | `SENSITIVE_PATTERNS` extended with 40-hex EVM wallet pattern; new `SECRET_KV_PATTERN` for key-name scrubbing (`apiKey=`/`authorization=`/`password=`/etc.); `hasConsent()` gate at top of `reportError`; `sanitize()` exported |
| 6 | `frontend/src/lib/analytics.ts` | R046 | `hasConsent()` gate at top of `track()`; `trackError()` routes message + context through shared `sanitize` from errorReporting |
| 7 | `frontend/src/lib/towelieKnowledge.ts` | R073 | 6 wrong claims fixed: 5/6→LP, 1/6→stakers; flat 25% early-withdraw penalty; binary +0.5× JBAC boost no-stack; 0.4× boost floor clarification; pull-payment pro-rata refunds |
| 8 | `frontend/src/lib/revertDecoder.ts` | R074 | viem `BaseError#walk()` for `ContractFunctionRevertedError` → `errorName`; full custom-error map (InsufficientPayment, InvalidLoanId, OracleStale, EnforcedPause, etc.); legacy string-pattern fallback retained |
| 9 | `frontend/src/lib/explorer.ts` | R074 | (already restored) zkSync, Linea, Scroll, Mantle, Blast, OP Sepolia, Polygon Amoy chains; `getChainLabel(chainId)` exported |
| 10 | `frontend/src/lib/txErrors.ts` | R074 | `isUserRejection` walks `.cause` chain (depth-capped at 8) so viem-nested rejections register correctly |
| 11 | `frontend/src/lib/pointsEngine.ts` | R036 | Self-referral guard in `incrementReferralCount`; `computeHashSync`/integrity-hash theatre dropped; helper `@deprecated` |
| 12 | `frontend/src/index.css` | R038 | `body min-height: 100vh` + cascading `100dvh`; nav-link `min-height: 44px` under `@media (max-width: 767px)` for WCAG 2.5.5 target size; body keeps `overflow-x: hidden` |
| 13 | `frontend/src/lib/abi-supplement.ts` + `contracts.ts` | R010 | Phantom `totalPenaltiesRedistributed` removed from `TEGRIDY_STAKING_ABI`; `extract-missing-abis.mjs` re-run, now also emits `TEGRIDY_STAKING_ABI_FULL` (144 fns) and `TEGRIDY_TWAP_ABI` (30 fns); `TEGRIDY_PAIR_ABI` includes `harvest()` per artifact |
| 14 | `frontend/src/lib/wagmi.ts` | R055 + R057 + R075 | viem `fallback([...], { rank: true })`; `rpc.ankr.com` confirmed in `connect-src` CSP |
| 15 | `frontend/src/lib/wagmi.ts` vs `constants.ts` | R057 + R055 | Re-scanned — no leaked secrets / API keys |

## Verification

```
$ cd frontend && npx tsc --noEmit
$ echo $?
0
```

Pre-existing strict-mode warnings in `pages/FarmPage.tsx` and
`pages/TreasuryPage.tsx` are unrelated (documented in R080 / R046) and
were not touched by this recovery pass.

## Notes

- Source-of-truth for `TWAP_ABI` is 30 functions per the forge artifact
  (prompt mentioned 28 — script reads the artifact, so 30 is shipped).
- `tegridyStakingAbi` (camelCase) lives in `src/generated.ts` and is
  regenerated by `wagmi-cli` from `TEGRIDY_STAKING_ABI`; `TEGRIDY_STAKING_ABI_FULL`
  in `abi-supplement.ts` exposes the full 144-function surface for callers
  that need view methods beyond the hand-rolled subset.
- No `.env` / secret files touched. No commits, no force-pushes.
