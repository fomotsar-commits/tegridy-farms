# R082 — LOW + INFO sweep + janitorial cleanup

## Source agents
092 V1V2Drift, 055 StaticDrift, 095 DocsDrift, 091 DeadCode, 101 FinalSweep —
the LOW / INFO residue after Waves 1–5 closed the HIGH/MEDIUM surface.

## Items addressed

### Stale V1 hook references (092)
- `frontend/src/pages/ContractsPage.tsx:328` — verified clean. Current copy
  reads "Frontend shows a 'V2 not deployed yet' empty state and reads are gated
  on `isDeployed()`." The "falls back to V1" claim called out by 092 has
  already been updated by an earlier pass; no edit needed.
- `frontend/src/hooks/useNFTDropV2.ts:159` — verified clean. Current line is
  inside an unrelated `resolveAssetUrl` block; the "see useNFTDrop comments"
  reference flagged by 092 no longer exists in the file (R043 / R071 deleted
  the V1 hook and the cross-reference at the same time).
- `frontend/src/lib/contracts.ts:378` — verified clean. Line 378 is now a
  struct field. The "same bytes4s as v1" comment that 092 flagged is gone.

Conclusion: 092 was running against pre-R071 source. The three call-outs are
already closed.

### Repo-URL typo sweep (`tegriddy-farms` → `tegridy-farms`)
Canonical from `git remote -v` = `fomotsar-commits/tegridy-farms.git`. Live
URLs corrected:
- `SECURITY.md:37` — `tegridyfarms/tegriddy-farms` → `fomotsar-commits/tegridy-farms`.
- `.github/ISSUE_TEMPLATE/config.yml` — both `tegridy-team/tegriddy-farms`
  links updated to `fomotsar-commits/tegridy-farms`.
- `docs/LAUNCHPAD_GUIDE.md:169` — Discussions URL.
- `docs/DEVELOPING.md:17-18` — `git clone` example.
- `frontend/src/pages/ContractsPage.tsx:360` — tracked-issues link (had
  reverted to typo since R035; re-fixed).

Cosmetic-only references left as-is (not URLs, no breakage):
- `package.json:2` — `"name": "tegriddy-farms-root"` is an internal package
  name string; safe.
- `README.md:202`, `DEVELOPING.md:12` — `tegriddy-farms/` is the on-disk
  layout directory diagram label, not a URL. The actual working tree on
  contributors' disks is named `tegriddy farms` (with space), so neither
  spelling is "correct" here — left alone.

### Stale audit dump files
`.audit_findings.md`, `.audit_full_force.md`, `.audit_my_eyes.md`,
`CODEBASE_FULL.txt`, `CODEBASE_OVERVIEW.txt` — already deleted from repo root
(staged delete in `git status` at session start). Equivalents preserved in
`.audit_101/legacy/audit_findings.md`, `.audit_101/legacy/audit_full_force.md`,
`.audit_101/legacy/audit_my_eyes.md`, `.audit_101/legacy/static_dumps/`. No
additional move needed.

### Stale `.bak` file (RECON1 follow-up)
- Removed `contracts/test/Audit195_Restaking.t.sol.bak` (50 KB). RECON1
  flagged this for deletion; the `.bak` had survived the original pass.
  Confirmed no other `.bak` files remain under `contracts/test/`.

### `.audit_101/REMEDIATION_REPORT.md`
- Created the index file with a 62-row table mapping each `Rxxx.md` to the
  finding one-liner, wave, and status. Adds a wave legend, status legend, and
  scope notes (what is intentionally NOT in the index).

## Items deferred

- `package.json` `"name": "tegriddy-farms-root"` — npm-internal name string,
  not user-facing, leaving as-is to avoid breaking any upstream consumer that
  pins it.
- `README.md:202` / `DEVELOPING.md:12` repo-tree labels — illustrative,
  on-disk layout is `tegriddy farms`. Neither spelling is "right", so no
  silent edit.
- 092 / 055 / 095 audit reports themselves still contain the historical
  `tegriddy-farms` strings inside their finding text. Editing audit reports
  in place would falsify the historical record; left as-is by design (the
  remediation sits in this `R082.md`, not in the agent reports).

## Verification

- `git status` post-edit: SECURITY.md, .github/ISSUE_TEMPLATE/config.yml,
  docs/LAUNCHPAD_GUIDE.md, docs/DEVELOPING.md, ContractsPage.tsx modified;
  `Audit195_Restaking.t.sol.bak` deleted; `.audit_101/REMEDIATION_REPORT.md`
  and `.audit_101/remediation/R082.md` added.
- `find contracts/test -name "*.bak"` empty.
- `grep -r tegriddy-farms` against live URLs returns only audit-report text
  (intentional historical record).

## Files touched

- `SECURITY.md`
- `.github/ISSUE_TEMPLATE/config.yml`
- `docs/LAUNCHPAD_GUIDE.md`
- `docs/DEVELOPING.md`
- `frontend/src/pages/ContractsPage.tsx`
- `contracts/test/Audit195_Restaking.t.sol.bak` (deleted)
- `.audit_101/REMEDIATION_REPORT.md` (new)
- `.audit_101/remediation/R082.md` (new — this file)
