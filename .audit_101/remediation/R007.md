# R007 — react-hooks/set-state-in-effect remediation

**Finding:** P0-HIGH (agent 096) — 16 `react-hooks/set-state-in-effect` errors
causing cascading renders / infinite-loop risk.

**Result:** **16 → 0** (`grep "react-hooks/set-state-in-effect" | wc -l`).

## Per-file changes

| File | Pattern | Fix |
|---|---|---|
| `src/pages/ActivityPage.tsx` | A | Removed `useEffect` + `useState`; tab derived directly from `location.pathname`. |
| `src/pages/LearnPage.tsx` | A | Same — derive `tab` from URL. |
| `src/pages/InfoPage.tsx` | A | Same — derive `tab` from URL. |
| `src/pages/HistoryPage.tsx` | A | Removed `useEffect(() => setPage(0), [txs.length])`; pagination reset moved into `fetchHistory`. |
| `src/pages/DashboardPage.tsx` | A | Removed `useEffect(setTab)`; derive `tab` from `searchParams`. |
| `src/pages/CommunityPage.tsx` | A | Same — derive `section` from `searchParams`. |
| `src/pages/LendingPage.tsx` | A | Same — derive `section` from `searchParams`. |
| `src/pages/GalleryPage.tsx` | A | Replaced `useEffect(setUserVotes, [address])` with React docs "store info from previous renders" (compare-during-render via `lastAddress` state). |
| `src/pages/AdminPage.tsx` | B | Replaced `useEffect(setConfirming/setTyped on isSuccess)` with `lastSuccessHash` ref + compare-during-render. |
| `src/components/FlashValue.tsx` | A | `lastValue` state + compare-during-render; `setFlash(null)` reset stays in async `setTimeout` (allowed). |
| `src/components/layout/TopNav.tsx` | A | Replaced `useEffect(closeMenus, [pathname])` with `lastPathname` compare-during-render. |
| `src/components/loader/AppLoader.tsx` | B | Skip-at-mount logic moved into `useState` lazy init (`shouldSkipAtMount()`); effect only fires `onComplete?.()`. |
| `src/components/chart/PriceChart.tsx` | C | Inlined `loadData` into the fetch effect; replaced `loading`/`error` state with derived flags from `loadedTf`/`errorState` so no synchronous setState happens in effect body. `cancelled` guard prevents post-unmount setState. |
| `src/components/TowelieAssistant.tsx` (×4) | C | All four event-driven effects (queue drain, wallet-connect, tx-success, wrong-network) deferred via `queueMicrotask` with `cancelled` guard. |

## Verification

- `cd frontend && npm run lint 2>&1 \| grep "react-hooks/set-state-in-effect" \| wc -l` → **0**
- Total errors dropped 127 → 88 (16 set-state-in-effect + collateral unused-import cleanup).
- No new errors introduced in any modified file (verified per-file with `npx eslint`).
- UI behavior preserved: URL-driven tab/section pages still respect deep-links + Back/Forward; FlashValue still flashes on value transitions; AppLoader still skips for repeat visits / reduced-motion; TowelieAssistant bubbles still fire for the same triggers (deferred by one microtask).

## Constraints honored

- No UI behavior changes.
- No subcomponent extraction needed.
- Battle-tested patterns from React docs ("you might not need an effect" + "store info from previous renders" + async cleanup guard).
