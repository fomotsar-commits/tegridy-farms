# R043 — Frontend Pool/Farm/NFT Hooks (Agent 062 HIGH/MEDIUM batch)

**Scope:** `frontend/src/hooks/{useNFTBoost,useNFTDropV2,usePoolTVL,useLPFarming,useFarmStats}.ts`
**Findings remediated:** H-062-01, H-062-02, H-062-03, H-062-04, M-062-05
**Tests:** 601/601 pass · `npx tsc --noEmit` clean

---

## H-062-01 · `useNFTBoost.ts` — chainId not in queryKey
- Added `useChainId()` + `chainId: CHAIN_ID` on every contract entry so wagmi's queryKey carries chain identity.
- Gate: `enabled: !!address && onMainnet` where `onMainnet = chainId === CHAIN_ID`.
- Off-mainnet → tri-state `null` (never `false`); the prior R034 boost-preview gate continues to work because confirmed-true is now further conditioned on `onMainnet`.
- Test default chainId is `1`, so `useNFTBoost.test.ts` needs no edit.

## H-062-02 · `useNFTDropV2.ts` / `useFarmStats.ts` / `usePoolTVL.ts` / `useLPFarming.ts` — chainId not in queryKey
- Same fix applied across all four hooks: `chainId: CHAIN_ID` on every read entry, `enabled: ... && onMainnet` on the batch.
- Eliminates the cross-chain refund-button leak called out in the audit (a wrongly-true `isCancelled` rendered on a chain where `refund()` reverts).

## H-062-03 · `usePoolTVL.ts` — unbounded TVL math
- Replaced `tvl = wethFloat * 2 * price.ethUsd` with a guarded `useMemo`-style computation:
  - `Number.isFinite(rawTvl)` and `rawTvl >= 0` checks.
  - `Math.min(rawTvl, MAX_TVL_USD)` cap at `1e12` ($1T).
- Stops a flash-loan-injected reserve (or NaN oracle) from selecting the high-side `dailyVolumeRatio` branch.
- New `usePoolTVL.test.ts` covers: zero-state, NaN price, $1T clamp, sane inputs, zero-TVL APR no-leak.

## H-062-04 · `useLPFarming.ts` & `useNFTDropV2.ts` — RPC storm
- `refetchInterval`: 30_000 → 60_000 in both hooks.
- Removed manual `refetch()` from each hook's `useEffect(isSuccess, ...)` toast handler — the 60s poll now drives state convergence; back-to-back txns no longer trigger a 10/12-call batch read storm.
- `usePoolTVL` poll bumped 30s → 60s for consistency (TVL doesn't move per-block).
- Existing tests do not exercise the success-side `refetch()` path; no test edit needed.

## M-062-05 · v1/v2 enum mismatch — eliminated v1 path
- `frontend/src/hooks/useNFTDrop.ts`, `useNFTDrop.test.ts`, and `frontend/src/components/launchpad/CollectionDetail.tsx` deleted (dead code per `project_scope_decision.md` — V1 contract source removed 2026-04-19).
- Verified live path: `LaunchpadSection.tsx` imports `CollectionDetailV2` only.
- Aligned `useNFTDropV2.ts` enum mapping to the **actual** contract enum (`contracts/src/TegridyDropV2.sol:27`):
  `0=CLOSED, 1=ALLOWLIST, 2=PUBLIC, 3=DUTCH_AUCTION, 4=CANCELLED`.
- Phase label for `0` corrected from "Paused" → "Closed". Behavior preserved: `currentPhase === 0` still gates mint disabled at the call site (consistent with contract `MintClosed()` revert).

---

## Files touched
- `frontend/src/hooks/useNFTBoost.ts` (chainId scoping)
- `frontend/src/hooks/useNFTDropV2.ts` (chainId scoping, 60s poll, drop refetch on success, enum label fix)
- `frontend/src/hooks/usePoolTVL.ts` (chainId scoping, 60s poll, NaN/Infinity/cap guard)
- `frontend/src/hooks/useLPFarming.ts` (chainId scoping, 60s poll, drop refetch on success)
- `frontend/src/hooks/useFarmStats.ts` (chainId scoping)
- `frontend/src/hooks/usePoolTVL.test.ts` (NEW — 5 tests for guard semantics)
- `frontend/src/hooks/useNFTDrop.ts` — DELETED
- `frontend/src/hooks/useNFTDrop.test.ts` — DELETED
- `frontend/src/components/launchpad/CollectionDetail.tsx` — DELETED

## Verification
- `npx tsc --noEmit` — clean
- `npx vitest run` — 36 files, 601 tests, all pass
- Manual: `LaunchpadSection.tsx` and `CollectionDetailV2.tsx` confirmed as the only live drop-detail surface; v1 paths fully unreferenced post-delete.
