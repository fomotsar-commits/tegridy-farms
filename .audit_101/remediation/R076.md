# R076 — Doc-drift sweep (post-R008 / R035)

**Agent:** R076 (RETRY pass — bulletproofing mandate)
**Date:** 2026-04-25
**Mandate:** Cross-check every numeric fact / address / function name in repo-root docs vs source of truth (`frontend/src/lib/constants.ts` for addresses; contract source for behavior). Do NOT touch artwork or personality copy.

---

## Sources of truth consulted

- `frontend/src/lib/constants.ts` — canonical contract addresses + staking constants (MIN_LOCK_DURATION = 7d, MAX_LOCK_DURATION = 4y, MIN_BOOST_BPS = 4000, MAX_BOOST_BPS = 40000, JBAC_BONUS_BPS = 5000, EARLY_WITHDRAWAL_PENALTY_BPS = 2500).
- `contracts/src/TegridyStaking.sol` — confirms each constant matches constants.ts (lines 58-64).
- `.audit_101/remediation/R003.md` — `TegridyLending` constructor now 5 args.
- `.audit_101/remediation/R015.md` — `POLAccumulator` constructor now 5 args + `LPMismatch`.
- `.audit_101/remediation/R020.md` — `VoteIncentives` constructor now 7 args (`_commitRevealFromGenesis`).
- `.audit_101/remediation/R029.md` — `TegridyNFTLending` no longer auto-whitelists; needs post-deploy `executeWhitelistCollection`.
- `docs/WAVE_0_TODO.md` §3 — confirms 3 contracts (LP Farming, Gauge Controller, NFT Lending) still pending multisig `acceptOwnership` from `0x0c41e76D2668143b9Dbe6292D34b7e5dE7b28bfe`.

---

## Files edited

| File | Change |
|---|---|
| `CHANGELOG.md` | Added new `[Unreleased]` entry "2026-04-25 — Wave 1-4 bulletproofing: ~80 HIGH/CRITICAL findings remediated" referencing `.audit_101/MASTER_REPORT.md` + `DETAILED_REPORT.md`. Lists the breaking constructor changes (R003 / R015 / R020 / R029) and the Wave 0 still-pending bucket. |
| `DEPLOY_RUNBOOK.md` | §1 inventory row for `TegridyLending` updated with R003 TWAP details ("constructor is now 5 args, was 4") + new "Subsequent batches" table with R015 / FeeHook / R020 / R029 rows. §10 closes the legacy "H-2 commit-reveal — not implemented" entry, opens "Wave-0 multisig acceptOwnership STILL OPEN" + "Wave-0 redeploys still pending broadcast" entries; supersedes the legacy "TF-13 orphaned bribes" with R020's `refundUnvotedBribe`. |
| `DEPLOY_CHEAT_SHEET.md` | Step 3 (V3 features) gains an R003 callout + `export TWAP=…` line. Step 4 (NFT Lending) gains R029 callout + post-deploy `proposeWhitelistCollection`/`executeWhitelistCollection` recipe for JBAC / Nakamigos / GNSS. Step 6 (Vote Incentives) gains R020 callout for the new 7th constructor arg. §3 "Post-broadcast wiring" gains a "Wave-0 multisig acceptOwnership STILL OPEN" callout (Step 0) + a new Step 5 for the R029 whitelist migration. |
| `NEXT_SESSION.md` | Header rewritten for 2026-04-25 status. "Immediate priorities" rewritten as 3 sub-buckets (1a multisig acceptOwnership on 3 contracts, 1b redeploys still pending, 1c R029 NFTLending whitelist migration). References `.audit_101/MASTER_REPORT.md` + `DETAILED_REPORT.md` + `R076` as this doc-drift sweep. |
| `FIX_STATUS.md` | Header refresh date and Wave-1-4 reference. "Needs YOU" section gains explicit Wave-0 multisig `acceptOwnership` line at the top + per-contract constructor-arg deltas (R003 / R015 / R020 / R029). |
| `AUDITS.md` | TL;DR table: internal AI-agent reviews count 7 → 8, with explicit reference to the **101-agent canonical pass (Apr 25, 2026)** under `.audit_101/MASTER_REPORT.md` + `DETAILED_REPORT.md` + `R001`–`R076`. |

## Cross-checked clean (no edits needed)

- `README.md` — Quick start section already references `DEPLOY_CHEAT_SHEET.md` and the deleted `redeploy-patched-3.sh`. Boost ladder + JBAC bonus + 1B fixed supply all match `TegridyStaking.sol` and `constants.ts`. Address tables match `constants.ts`.
- `TOKENOMICS.md` — 30/10/10/5/0/45 distribution split, 7d–4y lock range, 0.4×–4.0× linear boost, +0.5× JBAC bonus, 4.5× ceiling (`MAX_BOOST_BPS_CEILING = 45000`), 25% early-withdrawal penalty all match `TegridyStaking.sol` constants.
- `REVENUE_ANALYSIS.md` — `feeBps` is correctly described as a mutable state var (not `SWAP_FEE_BPS = 50` constant). R008 already corrected.
- `CONTRACTS.md` — Every address matches `constants.ts` (TOWELI, TegridyStaking, TegridyRestaking, TegridyFactory, TegridyRouter, TegridyLP, RevenueDistributor, SwapFeeRouter, POLAccumulator, LP Farming, Gauge Controller, Community Grants, Meme Bounty Board, Referral Splitter, Premium Access, Vote Incentives, Tegridy Lending, NFT Pool Factory, Token URI Reader, NFT Lending, TWAP, Fee Hook, Treasury, JBAC, JBAY Gold, WETH, Uniswap V2 Router/Factory).
- `FAQ.md` — Boost claim ("0.4×–4.0× boost on LP rewards depending on lock duration, plus an additional +0.5× if they hold a JBAC NFT (ceiling 4.5×)") matches contract constants. Multisig copy already says "Migration to a 4-of-7 Gnosis Safe is planned (Wave 0 incomplete as of 2026-04-25)" per R008.

## Constraints honoured

- **No artwork / personality copy touched** (per memory `feedback_preserve_art.md`). All edits are factual / numeric / version corrections.
- **No false claims added.** Every change reduces an existing claim toward on-chain or canonical-doc truth, or adds a verifiable callout (constructor-arg counts, post-deploy steps, multisig pending).
- **No `.env` content / secret values touched** (per memory `feedback_env_files.md`).
- **No new docs fabricated** — only edits to existing markdown.

## Top-5 corrections summary

1. CHANGELOG `[Unreleased]` entry naming Wave 1–4 bulletproofing + reference to `.audit_101/MASTER_REPORT.md` + `DETAILED_REPORT.md` + `R001`–`R076`.
2. DEPLOY_RUNBOOK + DEPLOY_CHEAT_SHEET: `TegridyLending` constructor is now 5 args (was 4) with new `_twap` arg (R003).
3. DEPLOY_RUNBOOK + DEPLOY_CHEAT_SHEET: `POLAccumulator` constructor adds `_twap` + `LPMismatch` factory check (R015); `VoteIncentives` constructor adds 7th arg `_commitRevealFromGenesis` (R020).
4. DEPLOY_CHEAT_SHEET + DEPLOY_RUNBOOK: NFTLending redeploy requires post-deploy `proposeWhitelistCollection` → 24h → `executeWhitelistCollection` per collection (R029); constructor no longer auto-whitelists.
5. NEXT_SESSION + FIX_STATUS + DEPLOY_RUNBOOK + DEPLOY_CHEAT_SHEET: Wave-0 still pending multisig `acceptOwnership` on 3 contracts (LP Farming, Gauge Controller, NFT Lending) by Safe `0x0c41e76D…b28bfe`.
